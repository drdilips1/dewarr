# Implementation status and evidence

Latest checkpoint: [Local list curation](LIST-CURATION.md) adds detail editing, atomic bulk catalog membership commands, conflict-aware ordering and explicit household sharing/revocation. Server pagination for large lists, supported write-back, usability and full stage acceptance remain open. The final checkpoint records the bounded verification.

Updated September 18, 2026. Objective remains **implement the full PRD end to end**, including the planned later capabilities. This is an implementation checkpoint, not a completion declaration.

## Current code

- [Local list curation](LIST-CURATION.md) supplies sparse detail edits, settings/content revisions, atomic bulk add/remove receipts, canonical membership and exclusion handling, reader-scoped sharing and revocation. Retried commands cannot replay over later membership episodes; removals preserve files and independent reasons. The list grid pages rendering only; full server pagination and large-list selector consistency remain open.

- [Series continuation](SERIES-DISCOVERY.md) projects loaded catalog series against accessible verified holdings, groups canonical aliases, distinguishes media gaps from overall ownership and exposes ordering/date/staleness uncertainty. The shelf uses batched local reads and creates no requests or downloads. Whole-library series discovery and full S08 qualification remain open.

- [Community lists](COMMUNITY-LISTS.md) adds public-list browse/search, fresh public-header validation, paginated book previews, current scoped ownership, private Follow receipts and atomic first-sync enqueue. Repeated follows preserve existing subscription/policy settings. New upstream membership uses the existing two-pass observation and list acquisition pipeline. Actual-account qualification, remaining discovery/curation and full S08 acceptance remain open.

- [Discovery](DISCOVERY.md) adds monthly Hardcover trending, recent work publications, related-title suggestions and recent local catalog shelves. Accepted visible provider IDs alone bind ownership; protected local metadata, grants and collection/media badges remain intact. Provider/account errors and stale cache have explicit states, while local author matches provide an attributed related-book fallback. Preview → catalog → list/request uses the existing services without automatic browsing side effects.

- [Reviewed omnibus import](COLLECTION-IMPORT.md) freezes selected complete child works beside the physical catalog edition. It uses the existing hardlink/copy publisher and establishes child ownership only after backend confirmation. Repeat import retains physical-edition deduplication; generic child requests do not inherit the collection's edition, language or narrator constraints. Stale child identities hold the operation, and changed physical-version/file evidence invalidates coverage. No migration beyond `0038_asset_containment` is required.

- [Verified collection contents](COLLECTION-CONTENTS.md) supplies an administrator review flow for a single observed omnibus item. Confirmed children share its backend link without fabricated files or editions. Changed evidence removes ownership and pauses compatible acquisition pending review; library grants and correction history remain enforced.

- [Exact version acquisition](EXACT-VERSION-ACQUISITION.md) permits ISBN-corroborated single-book recordings with matching complete narrator credits. Frozen version revisions and pending metadata conflicts fence preparation/dispatch; actual files still require independent version matching and backend confirmation. Generic exact-version packs and broader edition equivalence remain pending.

- [Native AudiobookBay](AUDIOBOOKBAY-INTEGRATION.md) supplies configurable source/proxy settings, rich posting observations, private verified artifacts, qBittorrent 5.2 metadata resolution and common manual/automatic acquisition. Unknown seed counts require an explicit automation opt-in and resolved metadata. Actual public-host/client certification and broader release equivalence remain pending.

- [List-derived series acquisition](LIST-SERIES.md) adds inherited Just book / Prefer packs / Complete reviewed series settings, finite backlog previews, future-entry scope resolution and originating-list links. Child selection and publication retain the originating policy generation and membership episode. Pause/resume, removal/re-addition and review changes have bounded lifecycle evidence. A post-download publication check rejects withdrawn origin even when another manual reason survives; a pause preserves already-submitted lifecycle work. Manual Complete-series requests still use the series page. Later automatic and manual reviewed-pack checkpoints add bounded incidental expansion; broader version/route/omnibus qualification remains pending.

- [Inherited route defaults](ROUTE-DEFAULTS.md) now carry a downloader and ebook/audio destinations through installation, personal, profile, list and request preferences. Manual/automatic selectors and list/series automation consume them while preserving explicit choices, current approval and frozen routes. Unavailable defaults never silently choose a replacement. This adds no migration; precise checkpoint evidence is recorded below. Inherited Complete series expansion and broader PRD qualification remain pending.

- [Automatic reviewed-series acquisition](SERIES-ACQUISITION.md) now connects an accepted finite series set to the existing search, selection, pack/reuse and importer pipeline. Each book retains independent authority and progress; held children can be retried beside progressing siblings, and catalog refresh cannot expand the accepted set. Verification is recorded in the checkpoint below. Inherited Complete series/list expansion, destination defaults and broader version/omnibus qualification remain pending.

- [Saved-transfer reuse](DOWNLOAD-REUSE.md) now adds independent join receipts and separate import continuations for later compatible same-owner pack requests. Targeted domain and real-file workflows verify one transfer, retained earlier import history, independent list authority, immediate/delayed ABS confirmation and saved-file failure recovery. Schema `0037_download_joins` is required. Broader source equivalence, cross-owner/mixed-route reuse and complete series/version/omnibus semantics remain pending; integrated checkpoint evidence is recorded below.

- [Automatic pack groups](AUTOMATIC-PACK-GROUPS.md) coalesce independently authorized compatible selections before dispatch. Each child retains its own request/list/route proof and fulfillment; the shared ledger consumes one transfer and capacity record. Later-request reuse is implemented separately above. Broader series scope and mixed-route/media coverage remain pending.

- [Automatic series preparation](SERIES-PREPARATION.md) now loads missing or stale Hardcover catalogs before source queries, shares active observations and records bounded fallback reasons. Fresh catalog/manifest evidence supports the existing single-target automatic pack selector. Automatic multi-target grouping, reuse and broader series/version policies remain pending.

- [Reviewed shared downloads](SHARED-DOWNLOADS.md) now connect up to 100 compatible selections owned by one requester to one physical transfer. Group dispatch is atomic; each child retains its reasons, requirements and fulfillment. Qualified children import independently, satisfied children are skipped, and ambiguous alternatives can be reviewed beside completed siblings. Automatic series/coverage selection, later/cross-owner reuse and omnibus handling remain pending.

- [Series catalog and curation](SERIES-CATALOG.md) now provides durable account-scoped Hardcover series observations, book-linked navigation, uncertainty labels, grant-scoped distinct ownership counts and selected additions to editable lists. Catalog refresh has no acquisition side effect; the selected list's existing authorized policy applies to additions. Reviewed finite target sets and same-owner grouped transfers have separate checkpoints; inherited automatic series policy, coverage selection and full version/omnibus fulfillment remain pending gates.

- Release-preference snapshots read all persisted layers in one database statement without retaining configuration locks across list/book workflows. Settings writes remain revision checked; final automatic submission fences edits until its durable marker commits. Cross-workflow concurrency evidence is recorded in the checkpoint below. No schema or API contract changes are required.

- [Download preferences](DOWNLOAD-PREFERENCES.md) resolve request overrides over list overrides, saved profiles, personal, installation and built-in preferences. [Request scope](REQUEST-SCOPE.md) adds inherited media, Either's first medium, language, abridgment, required narrators, standalone copies and required libraries. Preferred narrators rank eligible audio; required names constrain source selection, inspected imports and inventory satisfaction. Sparse inputs resolve into strict frozen requests; previews and saved request cards show actual scope and provenance. Series preferences, automatic destination-route defaults and reviewed changes to unsatisfied requests remain pending.

- List monitoring now reconciles membership re-additions and canonical merge/undo while retaining original monitoring and request records. New membership cycles reuse compatible held/submitted transfers and skip qualifying owned media. Paused additions still require selected catch-up, and withdrawn reasons remain independent. The monitor API/UI groups by canonical title without erasing original histories; see [List acquisition policies](LIST-POLICIES.md).

- [List acquisition policies](LIST-POLICIES.md) now provide Browse/Manual/Automatic modes, versioned activation previews, future-only baselines, explicit bounded backlog/catch-up, per-list media/profile/routes and independent authority. Scheduled source search and automatic selection feed the shared downloader/importer; paused or changed authority is checked before dispatch. Members require an administrator automation grant. Manual list requests inherit saved media, destinations and effective restrictions. Both/Either behavior, delayed library confirmation, shared-list deduplication and held transfer visibility have targeted evidence; full S06/S07 qualification remains open.

- [Request download restrictions](ACQUISITION-FOUNDATION.md#request-download-restrictions) retain independent blocked formats and whole-transfer size ceilings. Compatible planned reservations combine restrictions; selected/committed releases can be shared only when frozen manifest evidence satisfies new limits. Manual and automatic selection carry restrictions through the import profile; inventory ownership remains independent. API and list batches accept limits, while request/history/selection UI displays them. List policy activation and scheduled acquisition now have a separate checkpoint below.

- [Automatic selection and download](AUTOMATIC-SELECTION.md) now supports explicit `download_when_ready` consent in addition to preparation-only requests. Selection, trusted automatic attempt, claims and download enqueue commit together. Current route approval, requester/approver authority, original profile and catalog identity are checked before submission. Approval changes do not prevent observation of existing transfers or silently authorize importing under a replacement policy. Whole-torrent size limits include padding. Real EPUB/audio worker workflows verify hardlinks, delayed ABS confirmation, member fulfillment and replay with synthetic external services. Standing list policies and scheduled searches now have separate evidence below.

- [Transfer and storage limits](CAPACITY.md) now provide administrator settings, endpoint slots, a rolling internal automatic-transfer budget and shared filesystem reservations. Admission uses actual filesystem identity and transactional storage generations; inspected torrent bytes, future copies and per-child imports share accounting. Verified staging survives low-space waits/restarts without reserving a second copy. Existing download/import schedulers resume waits, while uncertain submissions keep capacity. Standing list policies now use this shared capacity service.

- [Reviewed list requests](LIST-REQUESTS.md) now select up to 100 titles, preview requested-media availability and compatible pending requests, and save ordinary wanted requests through an atomic durable worker. Independent reasons, cancellation before commit, expiry/retry, history, access redaction and mobile UI are implemented. This completes a manual batch-request subset; saved list media, destinations and restrictions now initialize its request form. Full inheritance across every acquisition surface remains pending.

- Goodreads RSS subscriptions now observe shelf additions through durable scheduled workers, conditional requests and shared cooldowns. Private provisional titles, corroborated ISBN/catalog reuse, independent local/source membership, durable exclusions, manual matching, pause/detach and owner-only settings are implemented. Feed omissions preserve earlier books and matched books use existing library availability. See [Goodreads subscriptions](GOODREADS-SUBSCRIPTIONS.md). Reviewed CSV snapshots now support mapped columns, shelf/subset selection, private bindings and durable receipts; see [CSV list import](CSV-LIST-IMPORT.md). [Hardcover subscriptions](HARDCOVER-LISTS.md) now add account-backed own/followed/public list selection, fresh two-pass membership verification, source-only removals, durable exclusions and private catalog reuse. Bounded policy activation/backfill and list-driven downloads are implemented separately; live-list and complete identity/collection qualification remain pending.

- Book-level Sources now combines durable MAM and Prowlarr results with independent source status, reload persistence, private saved download profiles and explainable format/source/seed ranking. Wanted requests carry through the shared torrent/selection path; frozen profile format and transfer-size limits apply at selection and import. See [Book source search](BOOK-SOURCES.md). Complete automatic eligibility, series coverage, ABB and full settings inheritance remain pending.

- Prowlarr now has encrypted administrator settings, sanitized per-indexer capabilities, private expiring results, incremental source browsing and torrent-file resolution into the shared acquisition path. Native-MAM overlap, source generation/permission checks, cooldowns and source-specific locking are enforced. See [Prowlarr integration](PROWLARR-INTEGRATION.md). Live Prowlarr certification, native ABB and full ranking/series automation remain pending; initial combined search is now implemented as described above.

- FastAPI API, React/TypeScript/Vite/Tailwind UI, generated OpenAPI client and locked Python/npm dependencies.
- Explicit Alembic migrations for identities, versions/representations/provider references, accounts/sessions, library bindings/grants, lists, operations and audit records.
- Procrastinate 3.9.0 schema in a separate namespace; enqueue on the same underlying psycopg transaction as domain changes.
- One-time token-protected bootstrap; Argon2 passwords; revocable opaque sessions; origin/CSRF checks; administrator/member/viewer enforcement; persistent sign-in budgets; authenticated secret encryption.
- Searchable/paginated local catalog, provisional title creation, availability projection, book detail, private/shared local lists, membership deduplication/order, account creation and durable activity checks.
- Responsive Seerr-style presentation independently implemented for the initial screens; no upstream frontend source has yet been copied.
- Audiobookshelf connection create/edit/disable/test with encrypted write-only tokens, URL prefixes, separate browser URL and scan capability diagnostics.
- Durable manual and scheduled ABS inventory with staged complete snapshots, pagination verification, permission/generation fencing, expired-run recovery, conservative missing detection and confirmed cross-library moves.
- My Library and book-copy views, medium-specific availability, private library grants, ABS deep links and durable manual work matching/unmatching. Recording evidence changes are held for review; matching narrator names alone never merge recordings.
- Hardcover/Open Library catalog adapters; paged search/edition loading; encrypted per-user Hardcover accounts; shared credential rate budgets, persistent cache and explicit provider errors/fallback.
- Provider search/preview/import UI, edition/recording cards, provenance, protected metadata edits, catalog-source matching/refresh, cover selection and simple/advanced metadata preferences. Catalog import never implies ownership or starts a download.
- Reversible administrator asset matches, source unmatch and changed-edition keep/separate review, with journaled history, optimistic revisions, concurrent undo and immutable library-version bindings.
- Automatic targeted Open Library enrichment for missing Hardcover work fields, with atomic job creation, protected/primary-value preservation, run-token fencing, permission/identity rechecks, provider-directed retry delays, orphaned-job recovery and book-detail status.
- Wanted media requests with normalized ebook/audio/both/either requirements, exact recording/edition constraints, independent manual/local-list reasons, compatible destination reservations, cancellation and periodic inventory/permission reconciliation. Book-page preview and persisted request history explicitly distinguish saving wanted media from downloading it.
- Reversible canonical book grouping with merge preview, inherited source/version views, canonical search/list/availability projections, original binding preservation and atomic planned-request reconciliation. Private origin metadata/grants remain scoped; existing URLs resolve to the selected main record.
- Administrator naming presets/token preview and saved settings; read-only completed-download inspection with actual EPUB/PDF/CBZ/audio evidence, source hashes, bounded traversal/probes, proposed recording groups, version mapping and immutable review plans. Owner-scoped API/UI, transactional queueing, generation fencing and stale-settings checks; see [Organization planning](ORGANIZATION-PLANNING.md).
- Configured destination bindings and generation-fenced real filesystem probes; journaled no-replace hardlink/copy publication with interruption recovery. Reviewed import API/UI, per-child states and shared-library version reservations, final permission/configuration/identity fencing, source-preserving retries and ABS-confirmed availability are now implemented. Resolved collection children proceed while uncertain children stay held.
- Eight actual ABS 2.36.1 native scanner/API cases and eight application workflows passed with synthetic media; see [native certification](ABS-NATIVE-CERTIFICATION.md). EPUB, PDF, CBZ, equivalent ebook formats, two-track audio, PDF companions and single-file cases include API dispatch, durable worker, exact-version confirmation, inventory refresh and duplicate skip.
- Immutable collection-group review with merge/split, disc/track assignment, explicit exclusions, proposal restore, stale-plan rejection and protection of reserved groups during sibling correction. Multi-file audio confirmation requires the reviewed playback order.
- Recoverable per-entry import cancellation, journaled staging cleanup, reservation release only after reconciliation, and selective replanning beside unchanged published siblings. Ambiguous publication stays held or continues ABS confirmation; see [Import cancellation](IMPORT-CANCELLATION.md).
- Initial cover export now freezes selected URLs and normalized JPEG bytes, validates public image routing and observed Open Library Archive redirects, and preserves later library artwork edits. Optional artwork failures leave books importable; see [Cover export](COVER-EXPORT.md).
- PDF/CBZ byte inspection runs in bounded child processes. Group review supports explicit same-edition ebook formats and PDF audiobook companions; companions do not establish ebook ownership or create catalog editions. Verified imported formats survive ABS inventory refresh only while file evidence agrees. See [Ebook formats and companions](EBOOK-IMPORTS.md).
- Local catalog matching now combines validated embedded edition identifiers with title/author and recording evidence. Review exposes candidates/conflicts, applies clear matches without asserting completeness and freezes revalidated evidence in the import plan. Manual matching remains available; see [Import matching](IMPORT-MATCHING.md).
- Native MAM connection/search/detail API and Sources UI now retain rich release fields, distinguish failures from empty results and protect encrypted session/proxy credentials. Cross-process leases, cookie rotation, generation fencing and durable cooldowns are fixture verified. Required HTTP proxy routing has a real local proxy test; live account compatibility remains unverified. See [MAM integration](MAM-INTEGRATION.md).
- Owner-only MAM artifact resolution and saved manifest views now validate v1/v2/hybrid torrent identity and file layout in a bounded native parser. Original torrent bytes are encrypted, source generations and actor changes are fenced, multi-hop cookie rotation/cooldowns are preserved, and stale manifests remain explicit. No dispatch occurs. See [Source artifacts](SOURCE-ARTIFACTS.md).
- Reviewed release selection now connects wanted requests to saved MAM artifacts, tested downloaders and verified import routes. Frozen reservation requirements support compatible sharing, deliberate Either-medium choice and cancellation/reconciliation without dispatch. Idempotent receipts, private history and settings/version staleness have API/browser coverage. See [Release selection](RELEASE-SELECTION.md).
- qBittorrent transport now supports current and legacy 5.x acknowledgements, bounded transfer/file observations, full v1/v2 identity and strict attempt/destination association. Seventy-six synthetic HTTP/contract cases cover lost responses, cancellation and unrelated transfers. Administrator settings now include encrypted credentials, generation-fenced connection tests, configurable path mappings and preview, stale worker-root detection and disable/edit flows; twenty-one API/database cases and the browser verify this configuration path. Dispatch remains off by default; the opt-in reviewed-selection route is described below. See [qBittorrent integration](QBITTORRENT-INTEGRATION.md).
- Opt-in durable qBittorrent attempts now persist identity claims and the submission boundary, recover crashes/lost responses through observation, preserve committed reservations, enforce current authority/settings and hand completed directory manifests to administrator inspection. API, Activity and synthetic browser submission are implemented; see [Download attempts](DOWNLOAD-ATTEMPTS.md). Changed-endpoint/path repair, shared-file reuse and full acquisition certification remain pending. Dispatch defaults off.
- Historical target fulfillment now links compatible requests to confirmed scoped assets and, when applicable, the producing import entry. Completed satisfied reservations retire while torrent claims remain active. Worker/event reconciliation, periodic repair, Activity availability recheck, current-access projection, canonical-merge preservation and guarded rollback have integration coverage; see [Download fulfillment](DOWNLOAD-FULFILLMENT.md). Changed-endpoint/path repair, withdrawn-owner resolution and shared-pack reuse remain pending.
- Reviewed connection repairs now preserve immutable selections while accepting updated MAM/qBittorrent/ABS credential generations only after existing-transfer association. Owner/admin preview, durable idempotent review, pending/applied/held history, generation/permission fencing, uncertainty/restart recovery and Activity controls pass integration and browser checks. Different endpoints/file routes remain pending; see [Download repairs](DOWNLOAD-REPAIRS.md).
- Administrator review handoff now connects completed member-owned downloads to scoped administrator inspections and the existing importer. Activity provides claim/reassignment and failed-inspection retry; immutable assignment receipts, stale-review fencing and current requester grant/reason checks extend through final publication. Member-to-admin real-file workflows confirm original-owner fulfillment without duplicate submission; see [Download reviews](DOWNLOAD-REVIEWS.md). Opt-in automatic continuation is now implemented for qualifying local catalog matches; the complete acquisition gate remains pending.
- Opt-in automatic destination policies now continue completed acquisitions through administrator inspection and the shared frozen-plan/publisher services. Unique trusted catalog matches, supported container/track evidence, exact manifest checks, original requester authority and final policy-generation fencing permit unattended EPUB/M4B/MP3 imports. Ambiguous/unsupported groups remain held; exhausted jobs retain a bounded retry budget. Policy API/UI, real-file member acquisition and recovery fixtures pass; see [Automatic imports](AUTOMATIC-IMPORTS.md). Resolution without embedded identifiers, broader pack handling and external-list acquisition remain pending.
- Automatic imports can now resolve a missing local edition through a durable requester-owned Hardcover/public ebook lookup. Bounded complete edition pagination, existing source/provenance attachment, protected rejections/edits, input/token/permission fencing and atomic continuation feed the same file matcher. Exact identifier candidates precede limited title suggestions. An independent advanced preference defaults on. Provider HTTP and real-file acquisition tests cover second-page ebook/audio matches, ambiguity, quotas, concurrent continuation and authority changes; see [Import catalog resolution](IMPORT-CATALOG-RESOLUTION.md).
- Single-file source selection now reaches inspection, destination probing, frozen plans and hardlink/copy publication without enumerating neighboring downloads. Existing directory receipt hashes remain compatible. Two acquisition-to-library integration cases and real ABS single-ebook/single-audio workflows pass; see [Single-file imports](SINGLE-FILE-IMPORTS.md).
- Production initial OPF generation is frozen in schema-2 import plans and exercised by the actual ABS scanner. Destination probes now verify ABS root mapping through an empty-folder challenge, library media/metadata settings and detection capabilities; credential changes invalidate evidence.
- Docker/Compose scaffold, CI checks, isolated native database/browser harness, notices/reuse ledger and developer guide.

## Stage coverage

| Stage | Status | Remaining gate work |
|---|---|---|
| S00 | In progress | Run container/PG18 certification; complete full adapter contracts and media/provider fixtures; operator backup scaffold; execute CI |
| S01 | In progress | Intents/reasons/planned reservations and request reconciliation implemented; complete external-state repair and shared-file reuse (dispatch, atomic enqueue and confirmed reservation retirement now implemented), complete version compatibility, selective work splitting and original source-attachment undo, complete grants/account management and recovery workflows remain |
| S02 | Metadata/catalog path implemented and fixture verified; remaining stage work | Live Hardcover certification; cross-provider version reconciliation and selective work splitting; complete series acquisition beyond the catalog browser, richer identifier search and complete list UI |
| S03 | Inventory path implemented and fixture verified | Complete ABS certification beyond the eight native scanner/API cases; change-event adapter; broader identifier/file-based move resolution; explicit repair/ignore/replacement intents |
| S04 | Reviewed manual import, local identifier matching, per-child publication/reservations/fencing, PDF/CBZ, same-edition formats, PDF companions, reviewed omnibus contents and ABS confirmation implemented | Broader metadata/source resolution; unattended omnibus contents verification; additional formats; file-alias reconciliation; full recovery and compatibility matrix |
| S05 | Native MAM browsing/session/artifact foundation, reviewed release selection, qBittorrent settings/path mapping/transport/association and qualifying automatic import continuation implemented and fixture verified | Live MAM/qBittorrent certification; source-to-catalog resolution without valid embedded identifiers; changed-endpoint/path repair, withdrawn-owner resolution, shared-transfer reuse and full transfer-to-library integration; complete manual acquisition/recovery gate |
| S06 | Native MAM/ABB/Prowlarr paths, combined search, inherited profiles and bounded reviewed/shared-pack acquisition implemented and fixture verified | Broader recording/edition, mixed-route/media and omnibus coverage; pending-policy revisions; release equivalence and actual-source/client certification |
| S07 | Inbound observations, reviewed CSV/batches and bounded standing policies with future-only/backfill/catch-up, scheduled search and single-book automatic acquisition implemented | Live list certification; broader identity correction and large-list reconciliation, full inheritance and source/series/recording coverage; complete unattended release qualification |
| S08 | Provider trending/recent/related discovery and local fallback implemented; complete stage pending | Series-continuation shelves, community curation, sharing/write-back, usability and actual-provider qualification |
| S09 | Pending | Full hardening, compatibility, recovery and release qualification |
| S10 | Pending | All six expansion work packages remain in scope and unimplemented |

No full stage gate is marked complete. Working isolated foundations are not substitutes for the complete required workflow.

## Verified evidence

This table retains earlier foundation evidence. Later dated checkpoints below specify newer changes and exact regression scope; historical test counts are not current whole-product acceptance.

Environment: macOS arm64, Python 3.13.14, local PostgreSQL 16.14, Procrastinate 3.9.0, Node 26.7.0 and ffprobe 8.1.2. These are actual local test versions, distinct from the planned production certification matrix.

| Evidence | Scope |
|---|---|
| 1,114 backend tests passing | Eleven new request/list preference cases cover complete release-preference precedence, explicit clears, independent constraints, compatible sharing, stale preview and replay, request-bound polling/selection, batch snapshots, automatic list handoff and guarded rollback. Older migration fixtures explicitly model legacy rows so their original guards remain exercised. Eighteen prior defaults cases cover per-field precedence, explicit clearing/restoration, sparse profiles, invalid values, private scopes, concurrent/stale edits, frozen search replay, legacy snapshots, list-policy changes, pre-dispatch versus running transfer behavior and the populated downgrade guard. Twelve new lifecycle cases cover membership re-addition, pause/catch-up, transfer reuse, owned-media skip, canonical merge/undo and withdrawn reasons. Twenty-eight prior cases cover standing list activation/baseline/backfill, authority changes, overlapping requests, pause/resume, scheduler rollback/timing, policy history and Both/Either decisions, plus four real-file list-to-library cases with delayed/immediate confirmation. The full regression retains the previous request-constraint, import, downloader, metadata, identity and permission coverage. Synthetic external services remain distinct from actual-service certification |
| Migration upgrade → downgrade to base → upgrade and Alembic drift check passing on isolated test database | Current thirty-two migrations and queue namespace; full empty base roundtrip, populated capacity backfill/guard checks and schema-drift check; populated correction/acquisition/merge/settings/inspection/import/grouping/cover/cancellation-history and source-session/artifact/selection/attempt/fulfillment/repair/handoff/automatic-policy downgrade guards tested; not an upgrade from a released production application |
| Frontend TypeScript and production build passing | Current implemented routes and generated API types |
| Fifteen browser journeys passing in one invocation | The new journey edits list rankings/formats, inherits them into a manual batch, changes its base profile without losing list attribution, applies a request override, saves through the real worker and verifies identical policy evidence in request-bound source search after reload. Desktop/mobile checks retain setup, accounts/grants, catalog, metadata/corrections, inventory, organization/import/cancellation, MAM/Prowlarr aggregation, source selection, downloader setup/repair, Goodreads/CSV/Hardcover observation, manual batches, capacity, automatic selection, scheduled list acquisition, canonical monitoring and personal/installation defaults. External HTTP services are synthetic fixtures; the administrator-review UI journey uses a mocked contract with separate backend coverage. Test-only authentication counters reset between scenarios in a guarded `_browser_test` database; production limits are unchanged. The full run passed in 8.5 minutes |
| Native ABS 2.36.1 certification: eight item cases and eight application workflows passing | Actual server/API/manual scanner, publisher hardlinks, conventional/nested version leaves and OPF metadata; native Node 24, synthetic media only. EPUB, merged two-track audio, PDF, CBZ, EPUB+PDF, audio+PDF companion and single-file ebook/audio pass application API/worker confirmation, correct catalog-version count, inventory refresh, ownership and duplicate skip. All eight include queued cancellation, independent covers and preservation of later artwork edits; watcher and the wider compatibility matrix remain pending |
| Read-only live Open Library lookup passing | Search for Frankenstein/Mary Shelley, resolve the returned work/authors and load 50 editions with truthful continuation; no broader provider/format certification implied |
| Read-only live Open Library cover retrieval passing | One documented cover fetched through the production public-IP/TLS-pinned route, including Archive redirects, then normalized in the decoder process; no external artwork bundled |
| Wheel build and frozen migration inclusion | Current Python packaging; single-file scope/publisher changes, native parser and current thirty-two migrations and explicit libtorrent/Boost/OpenSSL notices verified in the wheel |
| Registry-resolved image digests | Image identities exist; does not prove image build or Compose runtime |

Current backend evidence is `.local/evidence/request-preferences-full-final.log` (earlier JUnit evidence remains at `.local/evidence/backend.xml`); browser screenshots/results are under `apps/web/test-results/`. Generated artifacts and live database files are not committed. CI definitions are present but have not been run remotely.

## Acceptance mapping: partial evidence only

- AT-01: bootstrap/roles/origin plus ABS/MAM/qBittorrent connection subsets; qBittorrent encrypted settings, read-only diagnostics and mapping preview now have API/browser evidence. Complete setup and actual filesystem/client certification remain pending.
- AT-02: reversible asset matching/unmatching, source unmatch, changed-version review and stale/concurrent undo verified; canonical grouping/undo and historical binding preservation now verified; selective splitting and complete cross-provider version equivalence remain pending.
- AT-03: metadata provenance, manual locks/clear/unlock, fresh refresh, changed-narrator keep/separate review and targeted secondary enrichment verified through fixtures and the browser; full cross-provider and initial sidecar/ABS-edit preservation remains pending.
- AT-04/AT-05: local/provider catalog search, book page/editions and basic private/shared lists; source-native books, series navigation and discovery/sharing completion remain pending.
- AT-06/AT-07: synthetic HTTP inventory, private grants, media classification, partial-sync freshness, conservative missing confirmation and cross-library moves tested. Actual ABS scanner/application cases now also verify multi-format persistence and companion handling without false ebook editions; broader move/replacement and full compatibility scenarios remain pending.
- AT-12/AT-13/AT-30: wanted-media/exact-recording inventory checks, independent reasons, concurrent/replayed commands, compatible planned reservations, atomic enqueue/redelivery and periodic reconciliation verified; source ranking, actual transfers and the external-side-effect crash matrix remain pending.
- AT-12/AT-13 preparation subset: frozen candidate selection, fixed requirements, compatible sharing, Either-medium choice, cancellation, private history and stale route/version evidence have API/browser coverage. Preparation itself does not submit; the separately gated attempt route now does. Complete automatic eligibility remains pending.
- AT-13/AT-30 downloader subset: qBittorrent HTTP fixtures verify submission acknowledgements, no automatic add retry, lost-response observation and strict identity/tag/path association. Persisted restart/uncertain-submission recovery now has separate database fixtures; live-client certification and the complete transfer-to-library gate remain pending.
- AT-08/AT-13/AT-24/AT-30 repair subset: reviewed current connection generations are accepted only after readonly full transfer association. Atomic enqueue/rollback, duplicate/stale commands, generation/actor changes during network work, unknown/conflicting results, process death, successive repair, completion and downgrade guards pass fixtures. Browser settings edit/test/review/apply/reload and mobile layout pass; new endpoint or file-route migration remains unimplemented. Completed member files now use the separate administrator review path.
- AT-13/AT-19/AT-24/AT-27/AT-30 member-review subset: restricted administrator queue, idempotent/concurrent claims, reassignment, failed-inspection retry, worker/plan fencing, requester destination/version rules and actual grant/reason/user row locks pass PostgreSQL/API tests. Real temporary file workflows confirm member fulfillment through an administrator import; revocation after staging blocks publication. Browser queue transitions use an explicitly mocked API contract, separate from these real backend tests. Qualifying automatic continuation now has separate tests; full unattended acquisition remains pending.
- AT-13/AT-19/AT-24/AT-30 automatic-import subset: standing route approval, original-member fulfillment, exact manifest/unique identity checks, same shared planner/publisher, bounded retries, lost-callback repair, policy change/revocation and final-publication disable pass PostgreSQL and real-file fixtures. Actual ABS regression remains eight scanner/API cases and eight import workflows, not live automatic MAM/qBittorrent certification. Bounded provider edition resolution now has separate evidence; external-list automation and authorized series expansion remain pending.
- AT-13/AT-19/AT-24/AT-30 fulfillment subset: import confirmation atomically queues scoped request reconciliation; durable satisfaction links and completed reservation retirement retain torrent claims. Concurrent shared requests, wrong or inaccessible assets, current availability after removal/revocation, uncertain outcomes, legacy detached targets, canonical merge and rollback guards pass integration tests. This does not certify shared-pack reuse or changed-endpoint/path repair.
- AT-13/AT-30 attempt subset: atomic command/job rollback, concurrent/aliased replay, full identity claims, lease recovery, crash after add, lost response, unknown outcome, authority changes, cancellation, exact manifest/padding reconciliation, inspection enqueue and privacy now have database/API fixtures. Full external-state repair and live-client evidence remain pending.
- AT-10/AT-13/AT-27 Prowlarr subset: sanitized capabilities, source exclusions/native-MAM overlap, private expiring results, confined proxy resolution, immutable inspected artifacts and the shared acquisition/dispatch path pass fixtures. Initial cross-source search/ranking now has separate evidence below; complete ranking/series automation and live Prowlarr compatibility remain pending.
- AT-10/AT-12/AT-13/AT-27/AT-30 combined-search subset: owner-scoped durable MAM/Prowlarr operations, independent source failure/progress, private versioned profiles, transparent ordered ranking, source-generation/identity/permission fences, frozen selection/import restrictions and restart/retry handling pass unit/API/PostgreSQL cases. Complete automatic eligibility, coverage/series decisions, source cancellation and external-list policy inheritance remain pending.
- AT-20/AT-22/AT-27/AT-30 RSS observation subset: conditional feed requests, public pinned routing, private credentials/catalog visibility, persisted additions, source/local membership separation, omission retention, exclusions/restore, catalog binding and known-library availability, configuration/account fences, queue rollback/recovery and schema guards have synthetic API/database coverage. Six browser journeys now include the real observation and CSV workers. No complete external-list automation gate or live Goodreads compatibility is claimed.
- AT-08: native MAM search/detail, settings, required-proxy failure, safe errors, cookie rotation and cross-process lease/cooldown subsets pass fixtures/browser tests. MAM artifact resolution, owner-only persistence, native descriptor parsing and browser manifest views now have separate fixture evidence. Account-specific live checks and real transfer dispatch remain unverified; the browser now exercises synthetic dispatch. No complete MAM/acquisition gate is claimed.
- AT-15–AT-19/AT-30: naming, inspection, frozen plans, per-child publication, final authority/identity fencing, journal recovery and ABS confirmation subsets verified. Eight native ABS scanner item cases and eight application workflows pass; PDF/CBZ, reviewed equivalent formats and companion files now supplement grouping and playback-order evidence. Cover selection/preservation and cancellation/replanning pass their specific unit/integration/browser/native checks. Omnibus, watcher, additional formats and the complete recovery/scanner matrix remain pending.
- AT-16/AT-19 file scope: root/nested standalone EPUB acquisition reaches fixture-backed library confirmation with source-preserving hardlinks and no sibling import. Real ABS single-ebook/single-audio workflows confirm scope propagation through probing, publication, cancellation, duplicate skip and inventory refresh. Old directory receipts remain compatible.
- AT-16/AT-19: local identifier-based matching now has unit/API/browser evidence and an actual ABS EPUB workflow using a seeded catalog edition. Candidate suggestions and saved identity evidence do not imply full-content or library availability; broader automatic resolution remains pending.
- AT-24: operation activity, reviewed same-route connection repair and completed-member import handoff/reassignment/retry are implemented; qualifying automatic import continuation is now implemented; broader endpoint/path/import repair remains pending.
- AT-27: account/list/library isolation, permission revocation, sessions, write-only connection secrets and restricted endpoint redirects verified in the current subset; complete source/file defenses pending.
- AT-28/AT-29: responsive browser/build/migration subsets; full accessibility, packaging and compatibility certification pending.

All other scenarios remain not run. None of the full AT scenarios is claimed complete by these narrower tests.

- AT-12/AT-21 manual list-request subset: selected-book media preview, compatible pending targets, bounded atomic batch requests, independent reasons, cancellation before commit and durable receipts are implemented. See [Reviewed list requests](LIST-REQUESTS.md). Automatic list-to-download orchestration remains pending.

- AT-21 Hardcover subset: authorized list selection, complete paginated two-pass observation, source-only removals, exclusions, private catalog reuse, account fences and durable scheduling are implemented and fixture verified; live certification, policy activation and automatic acquisition remain pending. See [Hardcover subscriptions](HARDCOVER-LISTS.md).

- AT-21 CSV subset: reviewed mapping, encoding/bounds, identity dedupe, private snapshots, subset selection, additive membership, durable receipts, rollback/replay, account fences and RSS coexistence are implemented. See [CSV list import](CSV-LIST-IMPORT.md). This does not establish the complete S07 automation gate.

## Next work

Build automatic eligible pack coverage and inherited Just book / Prefer packs / Complete series policies on the explicit transfer-membership services in [Shared downloads](SHARED-DOWNLOADS.md). The reviewed batch API and independent child import are implemented subsets; later compatible request reuse, automatic group authority, mixed routes/media and omnibus containment remain required. Preserve the same transfer/reservation/fulfillment pipeline.

The shared manual list-batch path, bounded single-book automatic selection, explicit automatic-dispatch handoff, installation capacity accounting, durable restrictions, standing-list lifecycle and inherited request/narrator policies have implementation evidence. Next acquisition work is full series scope/coverage, automatic destination-route inheritance and reviewed policy revisions for unsatisfied requests, alongside native ABB and remaining source/compatibility gates. Preserve the existing list-to-library path while expanding it to collections and complete recording behavior. Passing the single-book fixtures does not accept full S06/S07.

Complete S01 identity and external-side-effect contracts, the remaining S02 metadata/series/list behavior, and S03 event/correction/live-compatibility gates. See [Acquisition foundation](ACQUISITION-FOUNDATION.md), [Metadata integration](METADATA-INTEGRATION.md) and [Identity corrections](IDENTITY-CORRECTIONS.md) for implemented contracts and remaining limitations. Complete ABS certification remains required before claiming general live support. Continue S04 from [Organization planning](ORGANIZATION-PLANNING.md) through grouping/format coverage, file-alias reconciliation and complete recovery/ABS certification. Native MAM browsing now supplies the source foundation for S05; qBittorrent transport and association now have fixture evidence. Downloader settings, read-only diagnostics and saved path mappings are implemented. Source artifact identity and private storage are implemented. Reviewed candidate-to-book selection is implemented with frozen requirements and route evidence. The opt-in attempt workflow now connects durable submission/observation to directory/file inspection. Confirmed fulfillment now retires completed request reservations while preserving transfer identity claims. Same-route connection-generation repair now resumes held transfers after reviewed readonly association. Completed member acquisitions now have administrator review with original-requester authority through publication. Automatic continuation now uses approved routes and unique local catalog matches, with bounded provider edition lookup where required. Next acquisition work needs resolution without embedded identifiers and broader pack coverage, changed-endpoint/path reconciliation, explicit withdrawn-owner resolution, shared-pack file reuse and full live client/import certification. Keep normal dispatch off until the dependent gates pass; S07 list automation remains gated on the complete acquisition path.

Known gaps to address as those modules land: complete list pagination instead of current bounded result windows; list rename/delete UI and bulk actions; selective work splitting and cross-provider version identity corrections; integration secrets/key rotation; role/grant administration and revocation; complete source/collection selection, standing policy scheduling and acquisition repair transitions; explicit external-state backup/restore.

## Inventory checkpoint limitations

See [Audiobookshelf integration contract](AUDIOBOOKSHELF-INTEGRATION.md). HTTP fixtures prove inventory/import workflow behavior; native certification covers eight specific scanner/file-layout cases and ebook/merged-audio application workflows. Neither is the complete compatibility gate. No user's ABS account, qBittorrent or media files were used. Live source acquisition, complete importing, external list automation and the rest of the PRD remain incomplete. Populated schemas with identity/privacy/correction/import history block lossy downgrade; use a pre-upgrade backup for rollback.


## Automatic selection checkpoint

[Automatic release preparation](AUTOMATIC-SELECTION.md) adds owner-scoped durable candidate selection through the existing acquisition selector. The Sources panel chooses verified routes, inspects at most five candidates, ranks actual primary-media formats, retains per-candidate reasons and opens the saved release for ordinary explicit dispatch. Forty-six targeted cases, the full 964-test backend suite and nine browser journeys pass. The new browser journey verifies a list-synced title through preparation and explicit synthetic dispatch; mobile layout was visually inspected. No migration is needed beyond schema 0027; generated API, lint/format, build, packaging and schema-drift checks pass.

This is partial AT-12/AT-13/AT-27/AT-30 evidence. Native ABB, complete recording/pack eligibility, shared-pack reuse and standing automatic list policies remain open. Capacity reservations now have their own checkpoint. Current automatic preparation never itself starts a torrent or marks a book owned.


## Capacity checkpoint

[Transfer and storage limits](CAPACITY.md) implements the shared capacity subset of PRD section 19 using schema `0028_capacity`. The full 994-test backend suite and ten browser journeys pass; the mobile settings screenshot was visually inspected. Generated API, lint/format, frontend/wheel builds, migration roundtrip/backfill/rollback guards and schema-drift checks pass. The local development database was backed up before migration; API and worker run the same revision with download dispatch disabled.

This is partial AT-12/AT-13/AT-19/AT-24/AT-27/AT-30 evidence. Standing list policies, automatic dispatch continuation, complete series/recording coverage, native ABB and remaining live-service/deployment certification are not complete. No full stage is accepted by this checkpoint.


## Automatic dispatch checkpoint

[Automatic selection and download](AUTOMATIC-SELECTION.md) adds explicit per-title automatic acquisition through the shared selector, downloader and importer. The full 1,016-test backend regression and eleven browser journeys pass. Four new real-file cases cover ebook/audio imports for member accounts, immediate/delayed ABS confirmation, source-preserving hardlinks and replay. Forty-four affected acquisition cases passed after the final wanted-status wording change. The mobile screenshot was inspected; final link spacing and wanted-panel wording received build/type/format verification. Generated OpenAPI/client, lint, wheel build and schema-drift checks pass. No migration beyond `0028_capacity` is required. API/worker must run the same revision; default dispatch remains off.

This is partial AT-12/AT-13/AT-19/AT-27/AT-30 evidence using synthetic source/downloader/ABS services with real local media files. Standing list policies, scheduled search/re-search, complete series/recording coverage, native ABB and live-service/deployment certification remain unfinished. No full stage is accepted by this checkpoint.

## Request restrictions checkpoint

[Request download restrictions](ACQUISITION-FOUNDATION.md#request-download-restrictions) preserve independent format and size requirements through compatible reservations, manual/automatic selection and frozen import profiles. Schema `0029_request_constraints` preserves legacy receipts and refuses lossy populated downgrade. The full 1,045-test backend regression and eleven browser journeys pass. Thirty-three targeted cases include the migration/replay proof and eight actual-file ebook/audio acquisition cases, four with request restrictions. The browser's automatic-dispatch journey seeds restrictions through the real API, verifies Wanted/selection summaries and the persisted receipt, and checks mobile overflow; its screenshot was visually inspected. This does not claim a list policy configuration UI.

Generated API/client, lint/format, frontend and wheel builds, migration inclusion, isolated empty-schema roundtrip and schema-drift checks pass. The development database was backed up before migration; API and worker run the new code with dispatch disabled. Evidence is in `.local/evidence/request-constraints-*` and the browser results directory. This is partial AT-12/AT-13/AT-19/AT-22/AT-30 evidence, not full S06/S07 acceptance. Continue with versioned list policy activation and scheduled search; native ABB, collection/recording coverage and production/live-service qualification remain open.


## Standing list policy checkpoint

[List acquisition policies](LIST-POLICIES.md) now connect owner-authorized future additions and selected backlog books to the existing search, automatic selection, download and import pipeline. Browse/manual/automatic modes, effective profiles/restrictions, media/routes, activation receipts, pause/catch-up, member automation grants and per-book monitoring persist in schema `0030_list_policies`. Original policy authority is rechecked before dispatch. Manual list requests inherit saved media/destinations/limits; compatible pending work appears in activation previews.

The full **1,073-test backend regression** and **twelve browser journeys** pass. Twenty-eight added cases include four real-file ebook/audio list-to-library workflows, immediate/delayed ABS confirmation, Both with an owned ebook, Either ownership skip and alternative-medium search, overlapping lists, independent withdrawal, explicit reauthorization, generation changes, stale previews, permission/privacy, scheduler rollback and minute-boundary jitter. The browser addition traverses actual application API/workers with synthetic Hardcover/MAM/qBittorrent services; source data and transfers are fixtures. Its mobile screenshot was inspected. Subsequent policy-summary polling and explanatory subscription copy passed the current TypeScript/build/format checks; the existing browser result does not claim a separate assertion of that polling behavior.

Generated OpenAPI/client, lint/format, frontend and wheel builds, current migration inclusion, isolated empty-schema roundtrip and drift checks pass. The development database was backed up to a restricted dump before upgrade; the restarted API and worker report schema `0030_list_policies`, ready health and a fresh worker heartbeat, with download dispatch still disabled. Evidence is under `.local/evidence/list-policies-*`; the full backend report is `.local/evidence/backend.xml`.

This advances AT-12/AT-13/AT-19/AT-20–AT-22/AT-24/AT-27/AT-30 subsets. It is not acceptance of full S06/S07. Membership re-addition and canonical merge/undo now have the subsequent checkpoint below. Remaining work includes broader identity correction, large-list reconciliation and full inheritance, native ABB, exact recording and series-pack handling/shared reuse, then discovery and full release qualification. Existing live account, container/deployment, recovery, performance and accessibility gates remain open. All S10 extensions remain in the full objective.


## List membership and canonical monitoring checkpoint

Monitoring distinguishes a new list membership from a provider refresh or canonical identity change. Removal marks baseline and active monitors removed immediately. Re-addition under an active policy creates a fresh command cycle, reuses compatible held/submitted attempts and skips qualifying owned media. Additions while paused require explicit catch-up. Merge/undo retains original monitoring and request rows, projects one canonical title and schedules one leader; a withdrawn origin cannot suppress another active request.

The full **1,085-test backend regression** passes. Twelve new lifecycle cases cover local/source-only re-addition, pause/catch-up, owned-media skip, transfer reuse, merge/undo and independent withdrawn reasons. Hardcover reappearance is tested through the shared membership functions, not a live remote account. The browser run passed the twelve existing journeys; its new thirteenth failed on a nested test locator. After a selector-only correction, setup and the new monitor journey both passed in a fresh test database. Its mobile screenshot was inspected. These combined results cover all thirteen journeys without claiming a single all-thirteen passing invocation.

Generated OpenAPI/client, TypeScript/production build, Python and frontend formatting, lint, schema drift and wheel inclusion pass. No migration is added beyond `0030_list_policies`; existing progress records have compatibility defaults. A restricted development backup was taken before restarting API and worker together; schema, ready health and fresh worker heartbeat were verified with dispatch disabled. Logs and the checkpoint manifest are under `.local/evidence/list-membership-*`.

This advances the S07 membership/identity lifecycle subset. It does not accept the full stage or certify live services. Full settings inheritance, broader identity corrections and large-list behavior, native ABB, exact recording and series-pack eligibility/shared reuse, discovery, release qualification and S10 remain in the full objective.


## Acquisition defaults checkpoint

[Download defaults](DOWNLOAD-PREFERENCES.md) now persist sparse installation/personal preferences and resolve each saved-profile field over those defaults. Existing complete profiles keep their explicit values. The reusable editor records only changed fields, supports returning individual values to inheritance and shows effective values/origins. Defaults use owner-only settings and administrator-only installation edits; invalid empty preference sets, stale concurrent edits and stale inherited previews are rejected.

The full **1,103-test backend regression** passes. Eighteen new cases exercise precedence, clear/reset semantics, sparse profiles, invalid inputs, private scopes and roles, concurrent edits, inherited stale previews, frozen search replay, old receipt compatibility, list reactivation, unsubmitted versus already-running automatic transfers, and the populated downgrade guard. The first browser run passed all thirteen prior journeys; its new defaults journey exposed cached personal values after installation edits. After dependent-cache invalidation and fresh-editor rendering, setup and the new journey passed together on a fresh database. The final build also clears saved notices after editing, waits for current profile data before reviewed selection and bounds numeric size conversion. The mobile defaults screenshot was visually inspected. This is fourteen journeys covered across runs, not an all-fourteen passing invocation.

Generated API/client, TypeScript/production build, frontend format, Python lint/format, empty migration roundtrip, schema drift and wheel inclusion pass. Schema is `0031_acquisition_defaults`. The development database was backed up before upgrade; API and worker restarted together and passed readiness/schema/fresh-heartbeat checks with dispatch disabled. Evidence is under `.local/evidence/acquisition-defaults-*`.

This advances S06-04/S07-04 and FR-20 preference resolution; it does not accept those stages. Complete request/list ranking overrides and inherited language/media/recording/series/destination preferences remain next in the plan, followed by remaining source/pack/identity, discovery and release qualification. Native ABB, actual account compatibility, the wider deployment/recovery matrix and all S10 extensions remain in the objective.

## Request/list release-preference checkpoint

Request overrides now take precedence over list overrides, selected profiles, personal defaults, installation defaults and built-ins for the six release-preference fields. Accepted intents and independent reasons retain snapshots. Manual list batches freeze their effective specification/policy; list automation carries its accepted policy into request-bound searches and selection. Independent size/format restrictions still govern compatible shared transfers. Old rows remain readable with unknown historical policy rather than fabricated backfill.

UI editors use collapsed list/request override controls with inherited values and reset actions. Preview/history show effective values and origins. A saved search is bound to its request; polling cannot switch to another request's newer search for the same work. Selecting a different request's search is rejected. Search controls reset when the book/request context changes. Choosing a base profile retains attribution for higher-priority list overrides.

The full **1,114-test backend suite** and **15-journey browser suite** pass. An additional eleven-case run verifies the final explicit size-rejection assertion. Four existing actual-file list workflows also now exercise a nonempty list ranking override through acquisition and import. Empty migration roundtrip, schema drift, populated downgrade guards, API/client regeneration, TypeScript/build, formatting/lint and wheel inclusion pass. The mobile request screenshot was visually inspected. Earlier browser failures identified a locator mismatch and accumulated test sign-in budgets; the final suite uses role-based lookup and isolates only test authentication counters. The reset helper refuses an ordinary development database.

Schema is `0032_request_release_policy`. The development database was backed up before upgrade; API and worker restarted together and passed readiness, schema and fresh-heartbeat checks with dispatch disabled. Evidence and backup location are recorded in `.local/evidence/request-preferences-checkpoint.json`.

This is release-preference inheritance, not completion of FR-20 or S06/S07. Remaining work includes language/media/narrator/series/destination inheritance and reviewed changes to unsatisfied requests. Code review at this checkpoint found differing configuration/list/work lock sequences; the subsequent concurrency checkpoint below records the reproduced deadlock, correction and tested boundaries. Native ABB, full pack/version behavior, discovery, live/deployment qualification and S10 extensions remain in scope.

## Preference concurrency checkpoint

A deterministic real-PostgreSQL interleaving reproduced `DeadlockDetected` between a manual request and a list batch: their transactions acquired configuration and book locks in opposite orders. Profile resolution now reads installation, personal and saved-profile columns in one statement without transaction-scoped configuration locks. Settings writers retain serialization and revision checks. Automatic dispatch takes the configuration fence only in its final submission transaction, rechecks current policy, and commits the possibly-external marker before releasing that fence and calling qBittorrent. Ordinary snapshots do not claim serializable multi-statement behavior.

The full **1,124-test backend suite** passes in 269.08 seconds. Ten new cases cover list batches interleaved with manual requests, reviewed selection and automatic preparation; committed and rolled-back multi-layer settings changes; concurrent profile edits; personal/installation edits during network preflight; and real PostgreSQL settings-writer waits until the submission marker commits. The targeted 44-case defaults/dispatch/concurrency run also passes. These cases qualify the named lock boundaries, not every possible workflow interleaving.

Python lint/format, unchanged OpenAPI/generated TypeScript contracts, package build, wheel inclusion and diff checks pass. No migration is added beyond `0032_request_release_policy`. No frontend code changed and the browser suite was not rerun; its prior 15-journey result belongs to the preceding checkpoint. A restricted development backup was taken; API and worker restarted together and passed ready/schema/fresh-heartbeat checks with dispatch disabled. The before-fix reproduction, current results and backup are recorded in `.local/evidence/preference-concurrency-checkpoint.json`.

This closes the reproduced preference-read deadlock and preserves the final automatic-dispatch policy boundary. Complete scope inheritance and reviewed edits to unsatisfied requests remain next within S06-04/S07-04–S07-06. Native ABB, complete series/recording handling, discovery, live/deployment/recovery qualification and S10 remain unfinished; no stage or full objective is declared complete.

## Inherited request scope checkpoint

[Request scope](REQUEST-SCOPE.md) now resolves desired media, Either's first medium, required language, abridgment, standalone copies and required libraries through the shared preference layers. Sparse `RequestOptions` become strict frozen `RequestSpec` values before access, ownership and reservation checks. Explicit any/clear/reset semantics remain distinct from omission. List configuration retains original scope and preference overrides for revalidation; an empty inherited scope stays empty instead of becoming a fully explicit request. Automatic routes remain separately approved and verified.

The UI supplies inherited media choices, a collapsed scope editor, and effective scope/origin summaries in previews and saved request cards. Browse-only lists need no acquisition setup: their fallback Either projection is labelled Browse inventory and does not authorize requesting or downloading. General source searches still serve compatible scope targets, while custom release overrides and explicitly selected request profiles retain their binding protections. Old request/batch commands can replay against their original defaults; existing accepted requirements do not change when defaults are edited.

The full **1,134-test backend suite** passes in 265.51 seconds. Ten new integration cases cover scope layering, explicit clearing, ownership versus Both, strict invalid options, stale previews, immutable/replayed requests, sparse batch receipts, list dispatch, profile substitution and browse-only authority. Four existing actual-file list workflows now inherit media, language and required library through download, import and immediate/delayed backend confirmation. Compatibility runs passed 65 and 38 targeted cases. Earlier failures exposed an outdated Either-default assertion and a source-binding check that incorrectly rejected general searches; the corrected suite preserves those workflows and their hard constraints.

The initial 16-journey browser run passed 14 and failed the new scope journey's exact-label locator and browse-only list setup. After role-based locator corrections and the browse-mode fix, setup, scope and identity monitoring passed together. A final two-journey run passed setup plus the completed saved-request scope display, including reload, explicit clearing/reset, frozen history and mobile overflow checks. Its screenshot was visually inspected. These are combined results across runs, not a single all-16 passing invocation.

Python lint/format, generated OpenAPI/client reproducibility, TypeScript/production build, frontend formatting, package build/current-module wheel inclusion, schema drift and diff checks pass. Schema remains `0032_request_release_policy`; no database migration was needed. Policy JSON and effective fingerprints expanded, so old previews/searches may need refresh and standing lists may need reactivation. Rollback requires the pre-upgrade backup, not an old binary reading new policy records. The development database was backed up; API and worker restarted together and passed readiness/schema/fresh-heartbeat checks with dispatch disabled. Evidence is recorded in `.local/evidence/request-scope-checkpoint.json`.

This advances S06-04/S07-04–S07-06 without closing those stages. Inherited narrator/recording preferences, series expansion, automatic destination-route defaults and reviewed revisions for unsatisfied requests remain. Library choices do not substitute for route defaults, and exact recording requests do not substitute for narrator preference. Native ABB, remaining source/pack/version behavior, discovery, live/deployment/recovery qualification and S10 remain in the full objective.

## Narrator preference and requirement checkpoint

Required and preferred narrators now inherit through the shared profile/default/list/request resolver. Required names are all-of audio constraints used for accessible inventory satisfaction, compatible reservation intersection, source eligibility, reviewed selection and import validation. Preferred names are soft ordered ranking; existing three-criterion profiles keep their order and use names as a tie-breaker until the user explicitly positions the narrator criterion. An ebook can retain overall ownership while an unsatisfied narrator-specific audio request remains wanted. Exact recording identity remains independent.

Selected transfer requirements stay frozen. A stricter later narrator reason shares only when existing requirements or the frozen exact recording establish the names. Source claims alone cannot establish this stronger reuse. Import plan creation verifies catalog and inspected audio credits; missing or conflicting required names remain held. Explicit semicolon-separated embedded credits preserve person-name commas and original author evidence. No source files are retagged or renamed. The settings and request editors expose add/remove/order, inheritance/reset and effective scope summaries.

The full **1,156-test backend suite** passes in 279.27 seconds with no skips. Twenty-two new cases cover conservative names, all-of semantics, required/preferred ranking, clearing, ownership, frozen sharing, selection, import checks with and without administrative handoff, actual audio credits and multiple narrator parsing. Existing actual-file single-book and list workflows now carry narrator requirements through automatic download, hardlink publication and immediate/delayed backend confirmation. Targeted runs passed 81 and 105 cases. The initial import run caught an author-credit regression in multi-narrator parsing; it was fixed and a preservation assertion added before the successful regression.

Two browser journeys pass: setup and the expanded scope journey with required/preferred names, ranking priority, explicit clear/reset, persisted receipts, reload and mobile overflow checks. The resulting mobile screenshot was visually inspected. The complete browser suite was not rerun for this checkpoint. Python lint/format, frontend formatting, TypeScript/production build, API/client regeneration reproducibility, wheel/current-module equality, Alembic drift, planning traceability and diff checks pass.

Schema stays `0032_request_release_policy`. Existing policy JSON can omit the new fields; newly expanded policy records require this binary. Refresh stale previews/searches and reactivate standing lists when their effective fingerprints differ. Rollback uses the pre-upgrade backup. API and worker restarted together after a mode-0600 development backup and passed ready/schema/fresh-heartbeat checks with download dispatch disabled. The revision, backup and verification paths are recorded in `.local/evidence/narrator-checkpoint.json`.

Next work remains complete series/pack scope and shared coverage, automatic route inheritance and reviewed changes to unsatisfied requests, plus native ABB and the remaining connector, discovery, deployment/recovery and S10 gates. These narrator results do not certify live accounts, complete recording equivalence, an entire stage or the full objective.


## Series catalog checkpoint

The series foundation now stages bounded keyset observations and verifies them with a second uncached pass before atomic publication. Failed or changing observations preserve the prior catalog. Account generation, endpoint, current permissions, operation supersession and worker leases fence stale work. Removed memberships retain history. Existing accepted bindings reuse canonical works; unresolved titles stay private and provisional. No refresh creates acquisition intents.

The browser links from Hardcover metadata, sorts numeric/unknown positions, retains duplicate/partial/compilation/merged evidence and separates raw entries from distinct book ownership. Counts obey library grants and canonical merge/undo. Published-book selection excludes uncertain entries; deliberate individual curation remains available. Selected additions show the destination's automatic-policy state, use bounded idempotent list commands and retain partial failures for retry.

The full backend run passed **1,196 tests in 305.38 seconds**, with no skips. After adding merge/undo, interrupted-job and publication-recovery assertions, the focused series suite passed **42 tests**; those additional assertions are recorded separately from that full run. Tests cover parser bounds, two-pass changes, same-name series, duplicate commands, account/permission changes during network requests, private catalog/inventory scopes, finite retries, atomic enqueue/publication failure and the populated migration downgrade guard. The initial API run exposed a deferred `updated_at` serialization read; the endpoint now explicitly refreshes its committed operation before serialization.

Two browser journeys pass against the real application and disposable provider/ABS/qBittorrent fixtures: the existing setup workflow and the new book → series → selected list additions journey, including mobile overflow and visual review. The initial series browser test referenced a title renamed by setup; its selector was corrected. Final UI polish was followed by a fresh successful run. The complete browser suite was not rerun. Lint/format, frontend production build, reproducible OpenAPI/client generation, wheel/module equality, documentation traceability and diff checks pass.

Migration `0033_catalog_series` passed isolated upgrade/downgrade/upgrade and schema-drift checks. The development database was backed up privately, migrated, and its API and worker restarted together. Ready/schema/fresh-heartbeat checks passed with download dispatch disabled. The operator contract is in [Series catalog](SERIES-CATALOG.md). Revision, backup, browser and packaging evidence is recorded in `.local/evidence/series-checkpoint.json`.

This completes a bounded series catalog/curation increment, not S06 or full acquisition. Next: reviewed finite series target sets; compatible shared-pack reservations/selection; verified child/omnibus fulfillment; automatic destination inheritance and reviewed policy revisions; native ABB, remaining live connector qualification, discovery/curation, production readiness and all S10 extensions.


## Reviewed series request checkpoint · September 18, 2026

The series browser now supports up to 100 selected books across pages, a reviewed finite main-book set, inherited media/release preferences, immutable previews and durable request history. Publication uncertainty and duplicate series positions stay visible. Decimal positions sort numerically in the preview; current overall/ebook/audio ownership stays separate from missing requested media. Main-book confirmation is user evidence, not a provider classification.

Accepted sets use the existing acquisition engine and compatible reservations. Each operation retains independent series reasons, frozen scope/preferences and per-book receipts. Preview acceptance rejects stale catalog generations, identities, expiry and effective defaults. Accepted sets do not expand after catalog refresh. Atomic worker retries cannot leave partial children or duplicate committed requests. Cancellation withdraws only that operation's reasons; manual/list/other-series requests and existing media remain intact. Queued cancellation is supported; individual reason withdrawal remains visible on the parent. Surviving series reasons authorize the existing reviewed import path.

Validation: the final full backend run passed **1,221 tests in 326.18 seconds**, with zero failures or skips. It includes 23 series-request cases covering identity/authority changes, concurrent retries, queue failure, atomic rollback, list/series overlap, cancellation, import authority, cross-account isolation, decimal ordering and populated downgrade protection. The final browser run passed **2 journeys**: setup plus the expanded series catalog/request/curation journey, including save, reload, receipt, cancellation and mobile screenshot review. The complete browser suite was not rerun. Backend lint/format, frontend formatting/production build, reproducible OpenAPI/client generation, wheel/module equality, local documentation links and diff whitespace checks passed.

Migration `0034_series_requests` passed isolated upgrade/check/downgrade/upgrade. The development database was backed up with mode 0600, migrated, and API/worker restarted together. Ready/schema/fresh-heartbeat checks passed with download dispatch disabled. The operator contract is [Reviewed series requests](SERIES-REQUESTS.md); revision and local evidence paths are recorded in `.local/evidence/series-requests-checkpoint.json`.

This implements the reviewed finite target-set portion of S06-05c, not the full task or stage. Saving these requests does not automatically select or download a pack. Remaining work includes inherited Just book / Prefer packs / Complete series policies across standing-list flows, destination inheritance, compatible shared-pack selection, verified child/omnibus fulfillment, live-provider qualification, discovery, production readiness and S10 expansion requirements. The full PRD remains active.


## Catalog-derived series source search checkpoint · September 18, 2026

Book Sources now expands an entered query into that query plus at most three known catalog series names. Accepted visible metadata and the current account's observed series memberships supply names and provider evidence. Names are normalized for search deduplication while their provider identities remain distinct. Unaccepted/private origins do not supply terms; changed membership, names or access invalidate a saved plan, while an unchanged metadata refresh preserves its frozen observation evidence. Legacy searches retain their single-query behavior.

The new `search_series` preference follows installation, personal, profile, list and request precedence, including explicit false overrides and reset to inheritance. Request-bound and standing-list source searches use the same resolver. Each MAM/Prowlarr indexer/query unit records independent progress and resumes without repeating completed work. Results deduplicate by source/indexer/release identity and preserve the first snapshot plus all query keys. Discovery terms do not establish book coverage, authorize additional downloads or create versions. The UI exposes query provenance, collapses detailed progress, and displays 50 ranked releases per page with focus restoration. API response-size/load qualification remains open.

Final validation: **1,233 backend tests passed in 322.68 seconds**, with zero failures or skips. New coverage includes alias limits/deduplication, inherited and list/request search overrides, MAM/Prowlarr rate-limit resume, independent failures, private merged origins, changing/unchanged metadata evidence, legacy operations and guarded downgrade. The first full run found a new test that incorrectly counted per-query Prowlarr capability checks and an existing lifecycle test with uncontrolled queue timing; both assertions/setup boundaries were corrected, then the focused 23-case suite and full regression passed.

The final **2 browser journeys passed** (setup plus series catalog/request/curation/source search), including query provenance, persistence after reload, entered-query-only override, a 51-result search split into 50/1 visible results, next/previous controls, focus restoration and mobile review. An initial ambiguous dropdown test locator was corrected to use its combobox role. The complete browser suite was not rerun. Lint/format, frontend production build, reproducible OpenAPI/client generation, wheel/module equality, local documentation links and diff checks passed.

Migration `0035_source_queries` passed isolated upgrade/check/downgrade/upgrade and schema-drift checks. The development database was backed up privately, migrated, and API/worker restarted together; readiness, schema and fresh heartbeat checks passed with dispatch disabled. The contract is [Series source search](SERIES-SOURCE-SEARCH.md); local evidence and revision are recorded in `.local/evidence/series-search-checkpoint.json`.

This closes the bounded catalog-alias discovery dependency of FR-24/S06-05d. Full Just book / Prefer packs / Complete series acquisition scope, compatible pack coverage selection, shared child reservations, per-child/omnibus fulfillment, destination inheritance and live-service qualification remain pending. S06/S07 and the full PRD remain active, including discovery, production readiness and S10 expansion requirements.


## Reviewed shared-pack checkpoint · September 18, 2026

Migration `0036_download_memberships` backfills existing attempts and records every reviewed selection served by one physical transfer. The batch command freezes at most 100 compatible same-owner, same-artifact, same-medium selections on one verified route. Deterministic work locks, atomic memberships/reservations/capacity/job creation, command receipts and original torrent identity claims preserve the one-submission boundary. Changed scopes and unrelated torrents are not silently adopted. Member lookup, same-route repair and list-monitor lookup use explicit membership.

Dispatch and completed-file continuation follow surviving wanted members independently of the representative. Whole-transfer cancellation before submission releases all child selections while preserving wanted requests; reason withdrawal stays scoped. Activity shows the individual books and media under one transfer. A request selected from another book's source page uses its own saved preferences rather than the original search policy.

Approved automatic import now accepts qualifying groups for distinct reviewed works, reassesses current missing demand and skips already-satisfied books without acquiring unnecessary alternate versions. Ambiguous alternatives for one work are held without blocking other qualified books. Reviewed repair imports only the unresolved child beside unchanged published siblings. Each version checks its own medium/language/abridgment/narrator/edition rules, and each target retires only after accessible ABS confirmation. Unrequested file groups do not gain import authority.

Focused verification: **20 shared-pack database and real-file workflow tests passed**. Six workflow cases use real synthetic EPUB bytes/hardlinks and an ABS HTTP fixture, including delayed scans, ownership arriving after queueing, ambiguous alternatives and selective repair. Fourteen dispatch/domain cases cover concurrency/replay, incompatible or foreign selections, cancellation, representative changes, child narrator requirements, uncertain submission, withdrawn groups and populated migration guards. The fixture scanner now retains stable item IDs when siblings publish in a different sort order.

The expanded setup/browser journey passed in 1.5 minutes and was visually reviewed on mobile and desktop. It saves two book selections, reviews/submits one transfer, reloads Activity and exposes both members. Initial browser failures identified a missing test navigation step and a missing semantic region for the new scope summary; both were corrected. Existing automatic-acquisition regressions also passed in the 28-case intermediate integration run. The completed full backend run recorded **1,252 passes and one failure in 327.91 seconds**. The failure was an existing cooldown assertion comparing a two-hour deadline against the later observation time; subscription-specific jitter can legitimately be zero. The test now bounds the deadline from the start of the failed sync and allows the documented jitter. All **15 subscription regressions then passed in 4.29 seconds**. Production cooldown behavior did not change. The full suite was not rerun after this test-only correction; its original failed result is retained.

A first full-suite attempt stalled after 676 passing tests during the new pack test's worker drain, with both imports completed, no blocked database transaction, no running job, and periodic jobs continuing to queue. It did not exit after SIGINT and was explicitly terminated. Diagnostics are in `.local/evidence/shared-pack-stalled-run.json`; that run is not counted as passing. The new finite workflow tests now isolate their queue from wall-clock periodic deferral and use polling without test-worker signal handlers. The cause of the shutdown stall is not certified; repeatable worker shutdown/periodic-race qualification remains an explicit S09 obligation.

Backend lint/format, frontend formatting/production build, reproducible OpenAPI/client generation, package build/module equality and diff checks passed. Planning validation confirmed 42 functional requirements, 12 nonfunctional requirements, 61 unique work packages, 35 acceptance scenarios, complete requirement traceability and no broken local file links in 12 checked documents. Migration upgrade/schema check/downgrade/upgrade passed on the isolated migration database. The development database was backed up privately before upgrade; API and worker restarted together on `0036_download_memberships`, with readiness, membership backfill and fresh-heartbeat checks passing and download dispatch still disabled. Local checkpoint evidence is recorded in `.local/evidence/shared-pack-checkpoint.json`.

This checkpoint does not close PACK-01–06/S06/S07 in full. Automatic pack selection/reuse and group authorization, inherited series/destination policy, broader version/omnibus coverage, native ABB, actual-service qualification, discovery, production readiness and S10 remain active. See [Shared downloads](SHARED-DOWNLOADS.md) for the API, migration and scope contract.


## Automatic catalog-pack coverage checkpoint · September 18, 2026

Automatic selection now freezes accessible series context and allows a qualifying series pack to supply the existing missing target. Primary filenames/folders must map unambiguously to known published catalog members, including the target; unknown/alternative/omnibus groups remain reviewable. The inherited `prefer_series_packs` default prefers qualifying packs and supports per-list/request opt-out. Whole-transfer limits remain bounded, child format ranking does not borrow a sibling's encoding, and generic pack narrators cannot establish a preferred child's narrator.

The operation retains explicit catalog/manifest evidence, checks changes after artifact resolution and before dispatch, and uses the existing atomic selection/attempt/capacity/import path. It imports only the authorized requested book. Other files remain in the seeded pack without new requests or owned badges. Legacy operations lacking pack context do not gain this capability through replay. The behavior and next integration work are documented in [Automatic series-pack coverage](AUTOMATIC-PACK-COVERAGE.md).

Verification: the full backend run passed **1,288 tests in 357.15 seconds**, with no failures or skips. The final additive API/UI change discloses separate single-book and eligible-pack limits while selection is queued. After that change, **86 relevant eligibility, selector, dispatch and real-file workflow checks passed in 31.15 seconds**. Those include 13 new pack-selection database cases, 17 pack-coverage unit cases, four requested-book pack workflows and the wrong-downloaded-identity case. The full suite was not repeated after this final receipt-disclosure adjustment.

Two browser journeys passed in 1.6 minutes: the setup/acquisition journey and list/request preference inheritance through source reload, including explicit pack opt-out. Mobile screenshots were reviewed for overflow; the complete browser suite was not run. Browser execution preceded the final receipt-limit wording change, which passed frontend formatting, generated-client and production-build checks. Initial focused failures were missing required fixture fields and an assertion expecting the transport's private validation detail in its generic public hold message; fixture construction and that assertion were corrected. The positive/revoked authorization cases and zero-submission assertion now pass.

Backend lint/format, frontend format/build, reproducible OpenAPI/client generation, package build/module equality, documentation links and diff checks passed. No schema migration or new service is required. The local API/worker were restarted together on the final code; readiness and fresh heartbeat checks passed with dispatch disabled. Exact revision and local evidence are recorded in `.local/evidence/pack-coverage-checkpoint.json`.

This is the pack-eligibility dependency of S06/S07, not full automatic series acquisition. Automatic missing-series catalog loading, multi-target automatic authorization/reservation, overlapping-list pack reuse, full series/destination inheritance, additional-version/omnibus coverage, native ABB, live integrations, discovery, production qualification and S10 remain part of the active full PRD.


## Automatic series-preparation checkpoint · September 18, 2026

Source searches now enqueue a durable metadata prerequisite before executing MAM/Prowlarr queries. Missing/stale Hardcover series catalogs share existing observations, including manual refreshes. Catalog publication uses the existing two-pass verification and provider budget. The source query plan freezes only after preparation, and source generations remain fixed. The inherited pack preference controls this behavior; no new configuration is needed.

Preparation waits at most ten minutes and exposes failures, changed connections and excessive cooldowns before falling back to ordinary search. Identity/reference changes stop the command. Freshness is enforced separately: series catalogs older than 24 hours remain browsable but cannot establish automatic pack eligibility. Loading a catalog never authorizes its additional books. See [Series preparation](SERIES-PREPARATION.md).

All **16 new integration tests passed in 5.18 seconds** after the final test-harness adjustment. They cover the real PostgreSQL queue/dependency/catalog/source path, replay, coalescing two searches with each other or a manual refresh, stale catalogs, opt-out, account changes, deadlines, identity/reference changes, failed/cancelled/long-cooldown dependencies, stopped jobs and frozen source generations. Earlier focused regressions passed 62 cases with one new harness failure; the latter was corrected to drain follow-on jobs explicitly.

The setup and expanded series browser journeys passed: **2 tests in 2.1 minutes**. The series journey now starts from Book Sources without manually refreshing the series, observes the metadata receipt, reloads it, and uses the resulting catalog for curation and source searches. The mobile screenshot was reviewed. The complete browser suite was not run.

The first full backend run was interrupted after 660 completed tests when the new finite worker test stopped making progress. Diagnostics showed completed catalog jobs, no blocked database transaction, idle sessions and continuing periodic deferral during shutdown. SIGINT exited the run. The test now isolates periodic tasks and uses polling without installing signal handlers, matching the existing finite shared-pack workflow tests. The interrupted result is retained in `.local/evidence/series-preparation-full-interrupted.log`; diagnostics are in `.local/evidence/series-preparation-stall.json`. This is test isolation, not a certified production worker-shutdown fix; S09 retains that obligation.

The final full backend run passed **1,304 tests in 339.10 seconds**, with no failures or skips. This run includes the final isolated worker test. Its complete output is in `.local/evidence/series-preparation-full-final.log`.

Frontend build/format, backend lint/format, reproducible OpenAPI/client generation, package build, equality of all eight changed packaged backend modules, documentation links and diff checks passed. No schema migration was required. Development API and worker restarted together with schema `0036_download_memberships`; readiness and a fresh heartbeat passed with dispatch disabled.

This completes the bounded missing-catalog prerequisite for existing single-target pack acquisition. Automatic multi-target authorization/grouping/reuse, full series/destination inheritance, broader versions/omnibuses, native ABB, live integration qualification, discovery, production gates and S10 remain active. The full PRD is not complete.


## Automatic pack-group checkpoint · September 18, 2026

Automatic pack selections now save a durable dispatch continuation. The coordinator waits up to one minute for already-authorized covered-book assessments, then groups compatible prepared selections using the existing shared-download ledger. It acquires command, operation, list, principal and canonical-work locks in a consistent order and validates each child's own proof. No series membership alone creates a request or import permission.

Every root records the shared transfer and explicit group selection IDs. One group commits one capacity record, torrent claims and dispatch job alongside its per-child reservations/memberships. Cancellation while waiting releases only that prepared child; current ownership skips unnecessary acquisition. Revoked catalog/route/profile/list proofs remain fenced. The manual download API cannot reclassify a waiting automatic selection to bypass automatic capacity accounting. Existing per-child import and ABS confirmation handle the group without changing seeded files.

The initial integrated run passed **50 cases in 41.76 seconds**, followed by **58 selector/dispatch/real-file/list/import regressions in 53.99 seconds**. The final ten-case domain run passed in 6.46 seconds before the later stopped-job recovery addition. An ownership-arrival assertion exposed that inventory evaluation had already cancelled the unnecessary prepared selection; the skip branch was corrected to recognize that state. The final four real-file workflows passed in **8.99 seconds**, covering two explicit requests and two overlapping lists, each with immediate or delayed ABS confirmation. They assert one submission, two memberships, independent list proofs, two fulfilled imports, hardlinks and unchanged source bytes.

The first selected browser run passed setup but could not run the automatic-download journey because its Hardcover shelf prerequisite had been omitted from the selected test set. The rerun includes that prerequisite. The automatic-download journey also checks the failed-coordinator cancellation UI through a response fixture; real cancellation/reservation release is covered by the database test. Browser execution of the new two-book automatic grouping itself is not claimed; its end-to-end evidence uses actual API/queue/filesystem/ABS-fixture workflows.

The implementation contract is [Automatic pack groups](AUTOMATIC-PACK-GROUPS.md). This checkpoint coalesces prepared automatic selections before dispatch. It does not yet join later requests to accepted/submitted transfers, share across owners or different routes/media, expand Complete series targets, or close broader version/omnibus coverage. Native ABB, live integrations, discovery, production readiness and S10 remain required parts of the active full PRD.


The full backend run passed **1,318 tests in 390.79 seconds**. After the final stopped-coordinator recovery message/button and regression were added, **40 focused coordinator/coverage/dispatch tests passed in 26.80 seconds**. The full suite was not repeated for that recovery adjustment. Backend lint/format, frontend build/format, unchanged reproducible OpenAPI/client generation, wheel equality for all eight changed backend modules, documentation links and diff checks passed. No migration is required. API/worker restarted together on `0036_download_memberships`; readiness and fresh heartbeat checks passed with installation download dispatch disabled.

A second browser attempt reached the automatic-selection page but exposed old completion/limit text assertions and an implicit dependency on a different test enabling the downloader. The journey now explicitly enables/tests its downloader and asserts the current source-search and transfer-limit text. These browser failures are retained; no passing result is inferred from the backend tests.

The final browser run passed **3 journeys in 3.0 minutes**: setup/shared-download regression, the Hardcover subscription prerequisite, and automatic download/reload/mobile/recovery controls. The full browser suite was not run. The new recovery control uses a UI response fixture while the corresponding prepared-selection cancellation is verified against PostgreSQL. Exact revision, logs and runtime checks are recorded locally in `.local/evidence/automatic-packs-checkpoint.json`. This remains a progress checkpoint, not full PRD completion.

## Saved-transfer reuse checkpoint · September 18, 2026

Later independently authorized automatic pack requests can now join the same owner's compatible saved transfer in queued, preflight, downloading or completed state. Each appended membership has a separate join-operation receipt; the original download payload and capacity/identity claim remain intact. A completed transfer receives a separate automatic import continuation, preserving the original import run and its publication authority. New import planning and publication stay restricted to the joined request scope. Existing reservations qualify only when their version and destination match.

The continuation rechecks current approval, surviving requests, qBittorrent association and exact completed manifest before reusing the inspection. Actual publication retains filesystem identity/hash checks and source-preserving hardlinks. Missing/renamed files hold without another torrent submission. Recheck retains historical timestamps and deduplicates live jobs; stopped retries become visible. Activity identifies later members, verification failures, publication review and confirmed joined holdings separately.

The full backend run passed **1,333 tests in 386.24 seconds**. After tightening existing-import version/destination qualification and adding a direct wrong-book publication-authority assertion, **41 focused reuse, automatic-acquisition and shared-import tests passed in 53.95 seconds**. A final live-preflight join regression was then added; **all nine domain reuse tests passed in 6.90 seconds**. The full suite was not repeated after those final changes. Earlier focused runs passed 27 acquisition/reuse cases and 25 coordinator/reuse/shared-import cases. Six real-file late-join cases cover explicit requests and overlapping automatic lists with immediate/delayed ABS confirmation, plus missing/renamed saved-file failure and repair. They verify one submission, distinct import runs, original receipt/inode preservation, unchanged seeded bytes and two final fulfillments.

The selected browser setup/shared-download journey passed in **1.5 minutes**, including a clearly identified response fixture for joined-member recovery display and mobile layout. The new two-book reuse workflows run through actual API/queue/database/filesystem behavior with synthetic external services; no browser-driven two-book flow or live provider certification is claimed. Frontend build/format, backend lint/format, reproducible OpenAPI/client generation, final wheel equality for 12 changed backend modules, local documentation links and diff checks passed.

Isolated migration verification passed fresh upgrade, model alignment, empty `0037` downgrade/upgrade and a populated valid-join downgrade guard. The rejected downgrade retained its schema and join membership. The development database and encryption key were backed up privately. An initial dump invocation using a URI in `PGDATABASE` failed before migration; retrying with separate libpq connection fields succeeded. API and worker stopped with no running development jobs, migration `0037_download_joins` and schema alignment passed, and both restarted together. Readiness, a fresh worker heartbeat and disabled installation download dispatch were verified.

The contract is [Saved-transfer reuse](DOWNLOAD-REUSE.md); exact logs/runtime/commit references are in `.local/evidence/download-reuse-checkpoint.json`. This closes the bounded same-owner, same-artifact, compatible-route reuse increment. It does not close broader source equivalence, mixed-route/media or cross-owner sharing, inherited Complete series scope, full recording/edition/omnibus handling, native ABB, live-service qualification, discovery, production readiness or S10. The full PRD remains the active objective.

## Reviewed-series acquisition checkpoint · September 18, 2026

An accepted series request can now automatically acquire its finite reviewed set. The parent saves per-book requests and one durable controller atomically. The controller reuses ordinary search, selection, shared pack/reuse, capacity, downloader and importer services. The series form offers tested downloaders and approved per-medium routes; saved history shows child progress and allows cancellation or retry beside progressing siblings.

Selection and dispatch carry independent series authority. Current automation permission, original accepted identity, surviving series reasons and frozen profile/route approvals are rechecked. A catalog refresh cannot expand the accepted scope. Per-book savepoints undo failed search/progress changes without stranding siblings. Successful finite sets stop after inventory confirms all requested targets. No schema change beyond `0037_download_joins` is required; API and worker must be deployed together to recognize the new controller task.

The focused run passed **39 tests in 52.41 seconds**, including ten controller/authority cases and the existing real-file acquisition suite. Two new real-file workflows acquire two ebooks from one pack with immediate or delayed ABS confirmation; they verify one submission, separate child fulfillment, unchanged source bytes and hardlinks. These workflows use actual PostgreSQL, workers and files with synthetic external services. Wider actual-provider, recording, mixed-route and omnibus qualification is not implied.

The full backend suite passed **1,346 tests in 412.82 seconds**. Backend lint/format, frontend build/format, reproducible OpenAPI/client generation, model alignment, wheel equality for all ten changed backend modules, documentation links and diff checks passed.

The first selected browser run passed setup but failed before series interaction because test navigation raced sign-in. The second passed setup but exposed a test readiness race while reading the saved automatic-import policy. Both test prerequisites now wait for their actual UI state. Their failed logs are retained locally. The final run passed **two browser journeys in 2.2 minutes**: setup/shared-download regression and series catalog/curation with automatic activation, progress, reload, cancellation and mobile layout. The automatic-series mobile screenshot was visually inspected. The browser scenario checks controller/UI behavior against real API/worker state; the two-book publication evidence comes from the real-file integration workflows above. The full browser suite and live external providers were not certified in this increment.

The development database and encryption key were backed up privately, and the dump catalog was verified readable. A preliminary backup assertion rejected an incorrect expected database name before writing a backup; the actual configured development database was inspected and used. The prior API/worker stopped with no running development jobs. Schema `0037_download_joins` and model alignment passed without a migration, then matched API/worker processes restarted. Readiness, a fresh worker heartbeat and disabled installation download dispatch were verified. Exact commands, logs and runtime/commit references are recorded in `.local/evidence/series-acquisition-checkpoint.json`.

The contract is [Automatic reviewed-series acquisition](SERIES-ACQUISITION.md). Inherited series scope and automatic route defaults, list-derived expansion, reviewed policy changes, full version/omnibus behavior, native ABB, discovery, live-service certification, production readiness and S10 remain required work. The full PRD remains active.

## Inherited route defaults checkpoint · September 18, 2026

Downloader and ebook/audio destination references now participate in the existing installation → personal → profile → list → request hierarchy. Sparse explicit nulls clear inherited choices; unset route fields stay absent from full snapshots to preserve legacy no-route revisions. Default/profile/request editors expose a collapsed route section and field origins. Manual and automatic release selectors, list policies and reviewed-series acquisition use the resolved choices. A missing or incompatible saved route cannot silently fall back to a different one.

List and series activation resolve omitted routes against current preferences, then validate accessible libraries, tested downloader/path mappings, verified destinations and current administrator approval. Accepted settings retain concrete route revisions and approval evidence. Lists also retain and return their original route options so reopening an editor preserves inheritance. Changed defaults or approval cannot silently rewrite an accepted acquisition or renew consent. No schema migration is needed.

The focused preference/route/real-file suite passed **54 tests in 49.94 seconds**, including four source-to-library workflows for list/series acquisition with immediate/delayed ABS confirmation using inherited routes. The full backend suite passed **1,357 tests in 369.93 seconds**. After the final list response field and reopening assertions, **25 route/list/series contract tests passed in 12.97 seconds**. The full suite was not repeated after that response-only addition. Backend lint/format, frontend build/format, reproducible OpenAPI/client generation, model alignment, wheel equality for all six changed backend modules, documentation links and diff checks passed.

The first browser run passed setup but could not locate the new route select by its exact label. Explicit accessible labels corrected that issue. The final selected run passed **two journeys in 2.2 minutes**, covering setup/shared-download regression and defaults save/reload/explicit clear/restore followed by inherited series acquisition, progress, reload and cancellation. These are real API/worker/browser flows with synthetic external services; the full browser suite and live-provider compatibility are not claimed.

The development database/key backup was saved privately and its archive catalog checked. API and worker stopped with no running development jobs, retained schema `0037_download_joins`, and restarted together. Readiness, a fresh worker heartbeat and disabled installation download dispatch were verified. Logs, commands, runtime and commit references are recorded in `.local/evidence/route-defaults-checkpoint.json`.

The contract is [Route defaults](ROUTE-DEFAULTS.md). This supersedes earlier pending statements about basic route-default inheritance. The next acquisition-policy work remains inherited Just book / Prefer packs / Complete series scope, list-derived finite expansion and reviewed changes to accepted requests. Full versions/recordings/omnibus coverage, native ABB, discovery, live-service certification, production gates and S10 remain required; the full PRD is not complete.

## Reusable main-book review checkpoint · September 18, 2026

Series pages now save a finite, owner-reviewed main-book selection separately from acquisition. The review records source/canonical identity, publication and membership facts, omitted works, revision and a fingerprint. Positions and featured flags do not establish main membership. Saving creates no requests or jobs. New catalog entries never enlarge the selection; relevant selected evidence changes require review, and stale observations require refresh. Withdrawal never revives an older revision.

Complete reviewed-series requests may explicitly reuse the current review for the exact selected set. Request acceptance serializes with review replacement/withdrawal and checks its frozen proof. Accepted requests retain independent reasons and scope after later withdrawal. API/browser views expose the reused revision, selected books, state and appropriate actions. Catalog publication is serialized before retaining review/request catalog row locks. The contract is [Reusable main-book reviews](SERIES-SCOPE-REVIEW.md); no schema migration beyond `0037_download_joins` is needed.

The initial focused series suite passed **56 tests in 16.25 seconds**. The review and real-file acquisition suite passed **58 tests in 55.69 seconds**, including two new saved-review workflows using inherited destinations, one shared pack, source-preserving hardlinks and immediate/delayed ABS confirmation. Withdrawal after request acceptance does not stop those independently authorized requests. These use real PostgreSQL/workers/files and synthetic external services.

The full backend regression passed **1,388 tests in 371.39 seconds** before the final catalog-publication lock guard and its new test. The affected series/catalog/real-file run after that guard passed **114 cases**, with one new concurrency test failing because its transaction-cached `pg_stat_activity` observation did not see the waiting connection. The test now reads live `pg_locks` records; it passed individually, then the entire **30-test review suite passed in 6.86 seconds**. The failed run is retained as `series-scope-final-first.log`. The full backend suite was not repeated after the final lock change; its affected paths have the focused results above.

Both selected browser runs passed **two journeys in 2.2 minutes**. The final run covers setup/shared-download regression, review save/reload/reselection, proof-backed request acceptance, review withdrawal without cancelling that request, existing automatic-series behavior and mobile layout. The expanded main-book review screenshot and request screenshot were visually inspected. The full browser suite and actual external-provider compatibility were not certified by this increment.

Backend lint/format, frontend build/format, generated API/client reproducibility, model alignment, wheel equality for the four changed backend modules, documentation links and diff checks pass. The development database/key backup was saved privately and the dump catalog verified. The matching API and worker restarted with no active development jobs; readiness, one fresh worker heartbeat, schema `0037_download_joins`, the new API route and disabled installation dispatch were verified. Logs, backup location reference, runtime and commit evidence are recorded in `.local/evidence/series-scope-checkpoint.json`.

This implements the explicit reviewed-evidence foundation of SC-01 and connects it to the existing finite-series workflow. Continue SC-02/SC-03 with inherited series scope and upstream list authority, then bounded Prefer packs/Complete series expansion and lifecycle qualification. It does not implement standing-list expansion, authorize downloads merely from catalog facts, close S06/S07, or complete the full PRD. All remaining source, version/omnibus, discovery, compatibility, production and S10 gates remain required.

## Inherited series scope and list-derived acquisition checkpoint · September 18, 2026

This increment builds on `0cb4649` and implements bounded SC-02/SC-03/SC-05/SC-06 behavior. [List series](LIST-SERIES.md) documents the contract and its remaining limits. It is progress toward the full PRD, not completion of S06/S07 or the overall goal.

Series scope now inherits through the shared preferences resolver, preserves legacy pack booleans/fingerprints and appears in settings/effective-value views. A Complete reviewed series automatic list resolves one finite main-book review, previews selected backlog children and creates a parent with the originating list, policy generation, root request and membership episode. The existing controller drives selection, compatible shared transfers, separate imports and ABS-confirmed per-child availability. Monitoring links to the series request and its originating list; manual single-book Complete previews direct users to the reviewed series page.

Lifecycle checks distinguish unaccepted previews from accepted scope, temporary pause from permanent withdrawal, and old membership episodes from re-additions. Missing main-book evidence holds only the affected root with a scheduled recheck. Scope planning does not wait behind a catalog publication while holding list/identity locks. Derived proofs participate in stable grouped list/series locking before selection and dispatch.

Review exposed a publication gap: a generic surviving request reason could otherwise allow a derived child's import after its originating list episode was withdrawn. New automatic publication checks verify the specific series/list origin and series reason. Publisher locks include upstream list/series authority and both root/child reasons through the publication guard. A pause retains the already-submitted transfer lifecycle; permanent removal holds new publication even when manual reasons survive. Published files and original seeded data remain unchanged.

Verification:

- Before the final publication fix: complete backend suite **1,402 passed in 381.36 s** (`.local/evidence/list-series-full.log`). This is baseline evidence, not a claim that a full suite was rerun after the fix.
- Final affected list, automatic-acquisition, review, importer and catalog-resolution regression: **117 passed in 94.70 s** (`list-series-import-regression.log`). Includes actual generated EPUBs, one two-book transfer, two hardlinks, immediate/delayed synthetic ABS confirmation, removal versus pause before publication, and real PostgreSQL root/child cancellation lock waits.
- Final shared-import, cancellation, capacity, grouping, reuse, shared-download, preference-concurrency and series regression: **134 passed in 53.68 s** (`list-series-shared-regression.log`).
- Browser journeys: **3 passed in 3.0 min** (`list-series-browser.log`): foundational setup/import/source flow, list/request preference inheritance, and series curation/review/list-derived request with reload and mobile layout. An initial browser run exposed an exact label locator mismatch; the corrected locator uses the observed accessible combobox role. The successful mobile series-request screenshot was visually inspected.
- Frontend build and formatting passed; Ruff passed across services/tests/scripts. Alembic reported no new upgrade operations. The built wheel matched all **182 backend Python modules**. OpenAPI and generated client include the new scope/origin response fields. All links in the 11 affected planning/feature documents and the 54-requirement/61-package/35-scenario traceability were verified (`list-series-docs.json`).

Deployment: a private database/key backup was created and its archive listing verified before restart. The backup path is recorded in `.local/evidence/list-series-backup-path.txt`. Development API/worker were restarted together with no active jobs at shutdown. Readiness returned **200**, one fresh worker was observed, schema remains `0037_download_joins`, and download dispatch remains **disabled** (`list-series-runtime.json`). No live tracker account or external library was used for certification.

Remaining scope: SC-04 Prefer-pack incidental child expansion; complete manual-surface integration; broader recording/edition/omnibus and route/media combinations; reviewed changes to unsatisfied policies; full multi-source and actual-service qualification; external-list release qualification; discovery/curation; production and S10. A main-book review and a passing list-derived pack journey do not establish these unfinished requirements.

## Automatic Prefer-pack child fulfillment checkpoint · September 18, 2026

This increment builds on `e1a462a`. [Pack expansion](PACK-EXPANSION.md) defines its bounded SC-04 contract and remaining qualification. Automatic Prefer packs now intersects a frozen main-book review with the chosen torrent's corroborated coverage, excludes the original work and creates independent requests for the selected medium. Each child rechecks owned/pending state and uses the existing series controller, shared membership ledger and importer. Receipts and series pages expose the expansion and its per-book progress.

A child uses the saved source artifact, with its original release observation, without another tracker query or torrent fetch. Both source identity and artifact checksum remain pinned. A more-seeded alternative cannot cause a separate transfer. Normal source-generation/access, catalog, route, file and import checks remain enforced. Root completion retains accepted child authority; root/list withdrawal fences new child publication even if another manual reason survives. Dispatch joins lock the original work alongside all child works in sorted order; publication locks the relevant root and child reasons. Reusable-review withdrawal and temporary list pause remain distinct from permanent request withdrawal.

Verification:

- Initial real-file tests exposed a missing second fulfillment: the root selection's `fulfilled` state was incorrectly treated as withdrawn authority. The correction passed immediate and delayed confirmation cases.
- Full backend suite before the final saved-artifact source path and additional audio cases: **1,416 passed in 397.23 s** (`.local/evidence/pack-expansion-full.log`). This is baseline evidence; the full suite was not repeated after those final changes.
- Affected acquisition/source/list/series/group/reuse regression after the saved-artifact change: **153 passed, 2 failed in 131.79 s** (`pack-expansion-final-regression.log`). Both new audio tests had unintentionally inherited a hard narrator constraint. The existing guard correctly rejected pack-level evidence that did not identify each recording. The ordinary audio-pack tests now explicitly request no exact narrator, and a separate test requires the constrained pack to remain held without any submission.
- Final focused scope, saved-observation, already-owned, actual ebook/audio file, delayed inventory, root/list withdrawal, pause, reusable-review withdrawal and concurrent root-lock/publication tests: **15 passed in 18.97 s** (`pack-expansion-media-final.log`). Successful real-file flows verify one qBittorrent submission, one torrent resolution, two distinct confirmed imports/fulfillments and unchanged source bytes with source/destination inode equality. A source fixture rejects any extra network call after initial resolution.
- Three browser regression journeys passed **3.1 min** (`pack-expansion-browser.log`): setup/import/source behavior, preference inheritance and series/list curation/acquisition with reload and mobile checks. The list-derived series mobile screenshot was visually inspected. These existing journeys do not claim a separate browser certification of every new incidental-pack receipt state. A subsequent copy-only clarification distinguishes Prefer packs from Complete series.
- Frontend build/formatting, backend lint/format, generated OpenAPI/client contracts and schema alignment pass. No migration is needed beyond `0037_download_joins`; the built wheel matches all **183 backend modules**. Planning links and 54-requirement/61-package traceability remain intact.

Deployment: a private database/key backup was created and its dump catalog verified. With no active development jobs, the API and worker were stopped and restarted together. Readiness returned **200**, one fresh worker was observed, the live API exposes the expansion response, schema remains `0037_download_joins` and dispatch remains **disabled**. Evidence is recorded in `pack-expansion-runtime.json` and the private backup path reference `pack-expansion-backup-path.txt`. Synthetic services and files establish these tests; no live tracker account or external library is certified by them.

Remaining work includes full manual selection integration, per-book exact recording/edition evidence (generic narrator/abridgment/version-constrained packs still require review), mixed-route/media and omnibus qualification, policy revision workflows, native ABB and complete source qualification, external-list release evidence, discovery/curation, production and S10. Neither this checkpoint nor its test count completes a parent stage or the full PRD.

## Manual reviewed-pack preparation checkpoint · September 18, 2026

This increment builds on `7d506a5`. [Manual pack preparation](MANUAL-PACKS.md) connects a saved manual release to additional covered reviewed books, atomically prepares their independent requests/selections and populates the existing shared-download action. Preparation does not submit to qBittorrent. A member can explicitly prepare a batch without permission to run standing automation. Existing compatible manual selections are reused; automatic, incompatible or committed selections retain their independent lifecycle.

The preview intersects reviewed catalog membership with the selected manifest, preserves medium, route and effective restrictions, and excludes the root. Changed scope, identity, ownership, preferences or frozen artifact evidence invalidates an unaccepted preview. Already satisfied children are skipped. The transaction takes command locks before sorted work locks and rechecks its evidence. Root cancellation while preparation waits wins before child writes. A failed child rolls back the complete batch. Batch fingerprints distinguish different subsets of unchanged pending work; repeated identical commands replay their receipt.

Verification:

- The initial two manual real-file tests found an extra generic `book.epub` left by the route-probe fixture alongside its titled replacement. The manifest correctly rejected the unidentified primary file. The fixture now renames that setup file; the matching rule was not weakened.
- Full backend suite before the final API translation of stored-artifact errors and three final error/authorization/lock tests: **1,436 passed in 436.47 s** (`.local/evidence/manual-pack-full.log`). This is baseline evidence, not a claim that the full suite was repeated after those additions.
- Final affected manual-pack, real-file shared import, selection, preference, shared-download and series-request regression: **95 passed in 44.68 s** (`manual-pack-final-regression.log`). This includes concurrent retries, stale evidence, corruption errors, another user's access/replay denial, automatic-selection isolation, overlapping pending subsets, atomic rollback and a verified PostgreSQL root-work lock wait followed by cancellation.
- Actual generated EPUB workflows prove one shared transfer, two independently confirmed books, preserved source bytes and matching source/destination hardlink inodes, with immediate and delayed synthetic ABS confirmation. Known but unrequested extra content is not silently imported. Existing shared-import regressions also cover already-owned books and ambiguous children.
- Three browser journeys passed **3.4 min** (`manual-pack-browser-final.log`): setup/import/source/shared-download flow, request/list preferences, and series curation/acquisition. The pack-review UI uses deterministic responses over real saved selections, then the real shared-download action; it does not claim independent browser certification of manifest matching. An initial browser fixture removed a handler during an in-flight refresh; retaining the scoped handler corrected that harness race. After a small mobile text-layout correction, the affected setup/shared-download journey passed again, **1 passed in 1.5 min** (`manual-pack-browser-layout.log`). The final mobile screenshot was inspected.
- Frontend build/formatting and backend lint/format pass. OpenAPI/client regeneration is reproducible; Alembic reports no new upgrade operations. The wheel matches all **184 backend Python modules**. Planning links and the 54-requirement/61-package traceability remain intact.

Deployment: a private database/key backup was created and the dump catalog verified (`manual-pack-backup-path.txt`). With no active development jobs, the verified API/worker processes were stopped and restarted together. Readiness returned **200**, one fresh worker was observed, both new API routes are exposed, schema remains `0037_download_joins`, and dispatch remains **disabled** (`manual-pack-runtime.json`). Synthetic services/files establish the workflow evidence; no live tracker account or external library was certified.

Remaining work includes broader manual source entry points and large/uncertain collection manifests; per-book exact recording/edition evidence, mixed routes/media and omnibus containment; reviewed policy revisions; native ABB and full source qualification; external-list release qualification; bookstore discovery/curation; production and S10. This increment advances SC-04 manual preparation and does not close S06/S07 or the full PRD.


## Native AudiobookBay acquisition checkpoint · September 18, 2026

This increment builds on `2dcc455`. [Native AudiobookBay](AUDIOBOOKBAY-INTEGRATION.md) connects configurable source/proxy settings and rich native search/detail to verified private torrent artifacts and the existing manual/automatic acquisition engine. Book searches aggregate ABB alongside MAM/Prowlarr; ebook-only searches do not query this audio source. Posting file lists remain claims, and catalog editions are not created from tracker options.

Magnet inspection uses qBittorrent's 5.2 metadata APIs, validates exported torrent hashes and paths, and never adds/starts a payload transfer. Older clients expose a capability error while ordinary torrent-file acquisitions remain supported. Source/downloader leases, persisted cooldowns, credential generations and current account permissions fence network results before persistence. Unknown seed counts require an explicit automatic-selection preference plus resolved metadata; they are not reported as zero or treated as proof of payload availability. Abridgment requires an explicit unambiguous posting label, not descriptive prose about another edition.

Verification:

- Full backend regression before the final abridgment-label refinement and six additional tests: **1,494 passed in 463.27 s** (`.local/evidence/abb-full-backend.log`). This establishes that revision's regression result; the entire suite was not repeated after that parser refinement.
- Final ABB parser, qBittorrent metadata, eligibility, source API and actual-file acquisition regression: **61 passed in 9.45 s** (`abb-final-regression.log`). It covers expired/private results, revoked viewers/accounts, proxy credential preservation/clearing, in-flight source/downloader changes, unknown seeds, malformed pages, leases/cooldowns, unsupported clients and wrong torrent identity.
- Real generated audio, real torrent descriptors and hardlink imports pass immediate and delayed synthetic ABS confirmation. Each successful flow submits once, preserves source bytes/inodes, excludes unrelated neighboring files and replays without another transfer. External downloader/ABS HTTP behavior is synthetic; this is not actual-service certification.
- Final browser run: **2 journeys passed in 1.8 min** (`abb-browser.log`): existing setup/shared acquisition plus ABB connection, rich results, posting claims, metadata inspection, common manifest reload and mobile layout. The mobile screenshot was inspected. Initial attempts exposed an ambiguous select label, a stale frontend bundle and the five-second assertion being shorter than the real source pacing. The label is explicit, the test waits for the bounded operation, and `npm run test:e2e` now builds the frontend first. A final proxy-help wording clarification does not change behavior.
- Backend lint/format, frontend production build/format, reproducible OpenAPI/client generation, schema alignment and diff checks pass. No migration is needed beyond `0037_download_joins`. The wheel matches all **188 backend Python modules**; new parsing dependencies retain their installed MIT notices. All **254** checked local documentation links/anchors and the **54-requirement/61-package** exports remain consistent.

Deployment: a private database/key backup was saved and its dump catalog verified (`abb-backup-path.txt`). With no active development jobs, the verified API/worker processes were restarted together. Readiness returned **200**, one fresh worker was observed, the ABB API is exposed, schema remains `0037_download_joins`, and dispatch remains **disabled** (`abb-runtime.json`). No live source credentials or external library were used in this qualification.

Remaining work: actual public ABB host and real qBittorrent 5.2 metadata certification; full source equivalence and broader source combinations; recording/edition, mixed-route/media and omnibus coverage; reviewed policy revisions; complete external-list release qualification; discovery/curation; production and S10. The connected ABB foundation advances S06 without closing its full gate or the full PRD goal.

## Exact recording acquisition checkpoint · September 18, 2026

This increment builds on `7d83f91`. [Exact version acquisition](EXACT-VERSION-ACQUISITION.md) allows automatic single-book recording selection when an explicit valid ISBN and complete narrator credits corroborate the requested catalog version. Equivalent ISBN-10/13 values normalize with checksum validation. Missing or contradictory evidence stays reviewable. Source releases and encodings do not create additional catalog versions.

Automatic operations freeze the catalog identity revision before inspection; new manual and automatic version-specific selections retain that revision through dispatch and import authority. Changed identifiers, publication year, narrator credits and other identity fields require fresh preparation. Pending provider conflicts block automatic preparation/dispatch. Legacy unsubmitted automatic exact-version records without frozen identity require fresh selection. Existing external transfers remain observable without resubmission, including after a catalog correction or while reconciling legacy selections.

History access and existing-transfer observation distinguish semantic corrections from permission revocation: exact-version visibility, account, route and source checks still apply. A corrected narrator conflicting with a required name pauses the request before import scheduling and preserves the completed transfer with a review message. A correction that leaves request constraints compatible reaches the frozen import identity guard. Neither path publishes stale media or marks the book owned.

Verification:

- Full backend baseline before the final observation/import authority refinements: **1,526 passed in 477.68 s** (`.local/evidence/exact-recording-full.log`). This is baseline evidence, not a claim that the complete suite was repeated after those refinements.
- Real generated audio and torrent workflows cover immediate/delayed synthetic ABS confirmation, exact catalog version assignment, stable version counts, one downloader submission, source byte preservation and hardlink inode equality. Wrong embedded ISBN or narrator is held despite matching source claims. Catalog corrections after submission preserve observation and prevent publication.
- The initial final regression exposed history/observation validation treating a semantic narrator conflict as lost access. The correction skips only semantic version constraints for read-only views and existing-transfer observation; exact-version authorization remains mandatory. A separate test withdraws accepted metadata and verifies the corrected recording becomes inaccessible again. Subsequent focused tests passed **10 cases in 8.66 s** (`exact-recording-access-fix.log`). The completion test distinguishes a paused request before import scheduling from an import receipt held by the frozen revision guard; neither condition is treated as availability.
- Existing setup/catalog/request/source browser regression passed **1 journey in 1.5 min** (`exact-recording-browser.log`). This exercises the existing recording-request UI and common acquisition surfaces; it does not independently certify the new exact-recording automatic flow in the browser.
- Final affected eligibility, recording, acquisition/request, download/repair/review, automatic import and shared-pack regression: **266 passed in 169.03 s** (`exact-recording-final-regression.log`). This run includes all final access/observation/import changes and the metadata-withdrawal privacy case.
- Backend lint/format, frontend production build/format, reproducible OpenAPI/client generation, schema alignment and diff checks pass. No migration is needed beyond `0037_download_joins`. The built wheel matches all **189 backend Python modules**; **149** checked local link targets in changed documents exist.

Deployment: a private database/key backup was created and its dump catalog verified (`exact-recording-backup-path.txt`). With no active development jobs, the verified API and worker were restarted together. Readiness returned **200**, one fresh worker was observed, schema remains `0037_download_joins`, and dispatch remains **disabled** (`exact-recording-runtime.json`). The new acquisition workflow uses synthetic external services and actual generated files; it does not certify a live tracker, qBittorrent instance or external library.

Remaining work includes per-book exact recording/edition evidence for generic collections, cross-provider edition equivalence, mixed routes/media and omnibus containment, reviewed policy revisions, full source and external-list certification, discovery/curation, production and S10. The native MAM explicit-ISBN path advances AT-12 and version-specific acquisition; it does not complete the full source/version stage or the PRD.

## Reviewed collection containment checkpoint · September 18, 2026

This increment builds on `f4aff5a`. [Verified collection contents](COLLECTION-CONTENTS.md) adds a complete administrator review flow for already observed inseparable ebook/audio collections. Selected complete works share one real backend item, become owned through existing library grants and link to that same item. The catalog/library UI labels collection ownership and lists verified contents. No child file, recording or edition is manufactured; standalone, exact-version and uncorroborated language/narrator constraints remain independent.

The proof freezes bibliographic and per-file identity evidence. Unchanged inventory preserves it; changed evidence invalidates coverage until explicit review. Compatible requests wait for that review rather than immediately downloading a replacement. Ordinary matching replaces collection coverage, and the correction journal can restore the previous decision only while its evidence remains current. Integration/asset locks and expected revisions protect concurrent review and inventory publication. Migration `0038_asset_containment` adds the nullable proof, with rollback guards for both live proofs and retained correction history.

Verification:

- Initial containment and correction tests: **17 passed in 6.02 s** (`.local/evidence/containment-initial.log`). They verify ebook/audio coverage, stable item/version counts, unchanged resync, metadata/file/missing invalidation, private grants, ordinary match replacement and undo.
- Full backend baseline before the final replacement-download guard and SQL-null/migration refinements: **1,541 passed, 1 failed in 481.41 s** (`containment-full.log`). The failure was an exact response assertion missing the new additive `in_collection: false` field. Its expected contract was updated; the full suite was not repeated after the final refinements.
- Final affected ABS inventory, containment, identity/catalog, work merging, requests, automatic acquisition, standing-list lifecycle, fulfillment, shared import and migration regression: **170 passed in 139.57 s** (`containment-final-regression.log`). This includes the corrected response assertion; concurrent reviews; waiting for invalidated coverage without claiming ownership; preserving standalone requests; and preventing stale undo.
- Migration verification downgrades/upgrades ordinary populated assets without losing their identity, confirms nullable proofs use SQL null, rejects rollback with live containment and separately rejects rollback when only correction history remains. The schema returns to `0038_asset_containment`.
- Browser verification: **2 journeys passed in 1.6 min** (`containment-browser-verified.log`), covering existing setup/acquisition and the new real-API collection review, acknowledgement, reload, shared backend link, mobile layout and undo. Initial attempts exposed an ambiguous history locator and a test reading before undo completion; the test now scopes the review form and awaits its successful close. The mobile screenshot was inspected. A subsequent UI-only handler refinement ensures ordinary matching and collection review cannot both remain open; the production build passes after that change.
- Backend lint/format, frontend production build/format, reproducible OpenAPI/client generation, schema alignment and diff checks pass. The wheel matches all **191 backend Python modules**; **179** changed-document local link targets exist.

Deployment: a private database/key backup was created and its dump catalog verified (`containment-backup-path.txt`). With no active development jobs, the verified old API/worker were stopped, migration `0038_asset_containment` applied and both services restarted together. Readiness returned **200**, one fresh worker was observed, the contents API is exposed and dispatch remains **disabled** (`containment-runtime.json`). This workflow uses synthetic ABS observations with file evidence; it is not certification of actual omnibus content or a live backend integration.

This checkpoint advances reviewed FR-26 containment for an existing backend item; automatic omnibus acquisition/import, per-child version evidence, mixed routes/media, policy revisions, broader source/list qualification, discovery, production and S10 remain required. It does not close a parent stage or the full PRD.

## Reviewed omnibus import checkpoint · September 18, 2026

This increment builds on `98d1351`. [Reviewed omnibus import](COLLECTION-IMPORT.md) carries an explicitly confirmed set of complete works through the existing inspected-file plan and publisher. The actual collection edition retains its version binding for physical ownership and repeat-import suppression. Contained works acquire generic ownership only after the one ABS item is confirmed; its recording, language and edition are not reused as child-version evidence. The UI keeps contents optional, requires explicit completeness acknowledgement, displays the frozen selection and identifies collection narrator credits separately.

Frozen child identity is checked before submission, publication and confirmation. Changed children hold the operation without deleting an already published item. Ordinary inventory preserves unchanged imported coverage and its physical edition; changed physical-version or file evidence invalidates it until renewed review. The existing inventory-only contents correction remains reversible and does not invent an edition binding. No additional migration beyond `0038_asset_containment` is required.

Verification:

- Initial new import and existing containment regression: **17 passed in 6.40 s** (`.local/evidence/collection-import-initial.log`).
- Full backend regression: **1,549 passed in 562.90 s** (`collection-import-full.log`). This run covered the final backend implementation before the additional audio variants and physical-version invalidation assertions were added to the tests.
- Final affected import, containment, request satisfaction and publication regression: **57 passed in 20.79 s** (`collection-import-final-regression.log`). The new collection module has eight cases, including actual EPUB/audio files, immediate/delayed synthetic ABS detection, preserved hardlinks/source bytes, one item/no invented versions, physical exact-edition satisfaction, child constraint separation, sticky physical-version invalidation, repeated commands and stale identity at three lifecycle points.
- Browser: **2 journeys passed in 1.6 min** (`collection-import-browser.log`). The extended setup/acquisition journey selects collection contents, enforces acknowledgement, reloads the frozen plan, delays backend detection and verifies child ownership only after confirmation. The separate inventory-review journey verifies correction/undo beside that imported collection. The mobile import-review screenshot was inspected.
- Backend lint/format, frontend production build/format, reproducible OpenAPI/client generation, schema alignment and diff checks pass. The wheel matches all **192 backend Python modules**. Planning links resolve; the backlog retains **61** packages and traceability retains **54** requirements.

Deployment: the private PostgreSQL/key backup and archive catalog were verified (`collection-import-backup-path.txt`). With zero active development jobs, the identified API and worker were stopped and restarted together. Readiness returned **200**, schema remains `0038_asset_containment`, one fresh worker is present and dispatch remains **disabled** (`collection-import-runtime.json`). Tests use real local files with synthetic external-service observations; actual native ABS omnibus certification remains open.

The end-to-end implementation plan now includes an explicit collection qualification packet distinguishing reviewed import, unattended contents evidence, per-target acquisition authority and native backend acceptance. Unattended omnibus verification/acquisition, broader constrained versions/routes, policy revisions, source/list certification, discovery/curation, production and S10 remain required. No full stage or full-PRD completion is claimed.

## Discovery shelves checkpoint · September 18, 2026

This increment builds on `4c49b1a`. [Discovery](DISCOVERY.md) adds a working `/discover` page with monthly Hardcover trending, recent work publications, and recent local catalog additions. Book pages also expose attributed related suggestions with an exact-author local fallback. A provider preview leads to the existing catalog import, editions, list curation and acquisition actions. Browsing alone creates no books or acquisition state.

Provider IDs are hydrated in bounded batches and matched only against accepted, visible catalog sources. Canonical duplicates collapse; ambiguous or unknown matches do not assert ownership. Protected local metadata remains on matched cards, and inventory is projected through current library grants even when the provider response is cached. Account generation changes fence in-flight results, and related lookup rechecks the seed's access afterward. Transient failures can show explicitly stale provider data; credential/permission failures cannot reuse that fallback. Local shelves remain independently usable.

The UI uses cover shelves, reason labels, keyboard-operable preview/close with focus restoration, mobile horizontal scrolling and a single disconnected-account prompt. Pagination appears only when needed. Visible shelves recheck availability periodically, respecting provider cooldowns and server-side metadata caching. Changing the catalog connection clears discovery queries. No new migration, runtime dependency or service is introduced.

Verification:

- Initial adapter/API suite: **33 passed in 4.52 s** (`.local/evidence/discovery-initial.log`). It covers ordered batched hydration, bounded date queries, malformed/partial provider replies, accepted IDs, canonical deduplication, private catalog/library grants, current inventory over cached metadata, outage states, account generation changes and side-effect-free browsing.
- All four production discovery queries validate against the official Hardcover GraphQL schema at `e8d38c8b7bd53cada7e97121ab0cacfa0804013c` (`discovery-schema-validation.log`). Validation used an ephemeral GraphQL parser; no project dependency was added. This establishes schema compatibility, not actual token-scope/service qualification.
- Full backend baseline: **1,583 passed, 1 failed in 584.39 s** (`discovery-full.log`). The existing withdrawn-list-reason test drained the entire worker before merge; due periodic jobs could create an outstanding download, which the production identity guard correctly rejected. The test now explicitly stops at its intended pre-dispatch boundary and asserts that no attempt exists. The production guard was not relaxed.
- Final discovery, metadata, identity merge and list-monitoring regression: **66 passed in 21.29 s** (`discovery-final-regression.log`). This includes the corrected test and an added case revoking a private seed's library access during provider I/O. The full suite was not repeated after the test-only boundary correction.
- Final browser verification: **2 journeys passed in 1.7 min** (`discovery-browser-verified.log`), including the existing setup/acquisition workflow and Discover → keyboard preview → catalog import → local list → matched card. It checks owned versus unknown cards, related attribution, release dates, mobile overflow and disconnected local browsing. Desktop/mobile captures were inspected and retained in `.local/evidence/discover-*.png`. An initial run exposed repeated attribution labels; redundant per-card text and unnecessary pagination were removed.
- Backend lint/format, frontend production build/format, reproducible OpenAPI/client generation, schema alignment and diff checks pass. The wheel matches all **194 backend Python modules**.

Deployment: a private database/key backup and its archive catalog were verified (`discovery-backup-path.txt`). With zero active development jobs, the identified API and worker were restarted together. Readiness and `/discover` return **200**; unauthenticated discovery data returns **401**. Schema remains `0038_asset_containment`, one fresh worker is present and dispatch remains **disabled** (`discovery-runtime.json`).

Remaining S08 work includes series-continuation shelves, community-list browsing/following from Discover, full local sharing, supported optional write-back, onboarding/usability and actual-provider/performance qualification. Earlier acquisition/collection/policy gates, full external-list certification, production hardening and all S10 packages remain in scope. This advances FR-11 and the discovery subset of AT-05; it does not complete S08 or the full PRD.

## Community-list discovery and subscription checkpoint · September 18, 2026

[Community lists](COMMUNITY-LISTS.md) connects Discover to documented Hardcover list search, current public records, scoped book previews and private local subscriptions. Follow creates its list, encrypted subscription, receipt and first sync job in one transaction. Repeated/concurrent follows reuse the existing subscription without changing paused or automatic settings. The existing two-pass worker and ordinary list policies supply baseline verification and automation; no external follow or read-status mutation is introduced.

The earlier `_ilike` query assumption was replaced with the documented `List` search operation. Search hits supply IDs only; current public list metadata is separately fetched. Member identifiers and book hydration are split to fit the published depth restriction. Missing public matches/books, unsupported sync size, malformed responses and unknown library matches remain explicit.

Verification at this checkpoint:

- Community adapter/API/transaction/acquisition, discovery, Hardcover subscriptions and list policy/lifecycle/monitoring regression: **101 passed in 58.86 seconds**. Evidence: `.local/evidence/community-final-regression.log`. This is the targeted affected suite, not a new full-backend-suite claim.
- The suite includes both ebook and audiobook workflows: public Follow → verified baseline → explicit future-only activation → new upstream membership → source selection → one transfer → real-file hardlink import → delayed synthetic ABS confirmation. Source bytes/inodes and repeated sync/follow deduplication are checked. The standalone acquisition run also passed **2 cases in 24.97 seconds**.
- Durable-worker first-sync coverage, concurrent command reuse, account/role changes during provider I/O, private-list cache rejection, scoped availability/follow references, recovery mode, oversized lists, enqueue rollback, detached receipts and subscription-command lock ordering pass. Invalid list IDs fail before provider I/O.
- Browser baseline plus community journey: **2 passed in 2.1 minutes**. Search, ownership, unknown-book preview/focus restoration, mobile overflow, Follow → actual local UUID route → synchronized private list, Browse defaults and existing-follow reopening are covered. Desktop/mobile screenshots were visually reviewed. Evidence: `.local/evidence/community-browser.log` and `apps/web/test-results/`.
- Five community GraphQL queries validate against the recorded official Hardcover schema. Production frontend build, generated client, Python lint/format, frontend formatting, wheel packaging with **197 Python modules**, and local documentation/traceability checks pass. No new runtime dependency or migration is needed.
- Development database/key/config backup verified with private file modes before restart. API and worker restarted on `0038_asset_containment`; readiness and community UI return 200, unauthenticated community data returns 401, one worker is fresh and download dispatch remains disabled. Evidence: `.local/evidence/community-runtime.json`.

Actual Hardcover account access/search ordering and live source/client acquisition remain independent qualification gates; fixture responses do not certify them. Remaining S08 work includes series-continuation discovery, complete curation/sharing, supported optional write-back and task-based usability. All unmet S00–S10 obligations remain in scope. This checkpoint advances FR-11–FR-12 and the S07/S08 connection; it does not complete the full PRD.

## Series-continuation discovery checkpoint · September 18, 2026

[Series discovery](SERIES-DISCOVERY.md) adds **Continue your series** to Discover. Each user's loaded Hardcover catalogs are filtered against accessible verified library assets. Canonical aliases and verified omnibus children retain the shared ownership semantics; future/unknown publications, compilations, partial records and removed memberships cannot become published gap recommendations. Ebook/audio filters retain the overall In library badge when the other medium exists. Series and book links use the existing curation and request flows; the shelf itself has no provider or acquisition side effects.

The scoped availability SELECT is extracted into a shared helper used by both ordinary availability projections and the series candidate query. Eligible series are selected before pagination; membership and ownership hydration is batched. The UI keeps four series per page and three title previews per series, with uncertainty labels, empty-state guidance, keyboard links and error/retry behavior.

Verification at this checkpoint:

- Affected integration regression: **175 passed in 51.87 seconds**, covering the new series shelf, series catalogs/requests/scope reviews, discovery/community lists, canonical merges, containment/collection importing, catalog lists and acquisition. Evidence: `.local/evidence/series-discovery-regression.log`. This is the targeted affected suite, not a full-backend-suite claim.
- The new API cases cover both medium filters, either-medium ownership, read-only behavior, stale observations, uncertain positions/dates, future/removed/compilation/partial records, private catalogs, member/viewer grants and revocation, disabled/inaccessible sources, canonical grouping, collection children, pagination and validation. A 1,255-membership fixture verifies fixed read count for one versus four series and three previews per card; it does not certify the full NFR performance dataset.
- Final browser baseline and connected series journey: **2 passed in 1.7 minutes**. Empty shelf → owned book → durable catalog refresh → fresh shelf → audio filter with ebook ownership → keyboard series link → existing request section/curation → actual local list, plus mobile overflow and error/retry. Evidence: `.local/evidence/series-discovery-browser-verified.log`. Desktop/mobile captures were inspected and retained in `.local/evidence/series-discovery-ui/`.
- The first browser run found that the global 30-second query freshness setting retained the empty shelf after a successful series refresh. The shelf now uses zero stale time and no inactive cache; the repeated journey verifies immediate navigation refresh. The fix changes product behavior rather than increasing the assertion timeout.
- Python lint/format, frontend production build/format and reproducible OpenAPI/client generation pass. The wheel matches all **198 backend Python modules**. No new migration, dependency, service or secret is introduced.

Deployment: a private database/encryption-key/configuration backup and readable archive catalog were verified (`.local/evidence/series-discovery-backup-path.txt`). With zero active jobs, the identified development API and worker were restarted together. Readiness and Discover return **200**, unauthenticated series data returns **401**, one worker is fresh and dispatch remains **disabled** on `0038_asset_containment` (`series-discovery-runtime.json`). Documentation checks validate **297 local links**, 54 requirement rows and 61 backlog packages.

This advances FR-11/S08-01 and the discovery portion of AT-05. Catalog refresh/provider responses in the connected browser journey are synthetic; actual-account qualification remains independent. Local sharing/curation, supported optional Hardcover writes, usability, broader recommendation coverage and full integration/performance/release gates remain open, as do all unmet S00–S10 requirements. No whole-library automatic series enrichment or completed PRD is claimed.


## Local list curation and household sharing checkpoint · September 18, 2026

This increment builds on `ed26c78`. [Local curation](LIST-CURATION.md) adds a name/description/sharing editor, catalog bulk selection, atomic add/remove commands and revision-aware ordering. A command validates the entire selection and commits memberships, external exclusions, withdrawn list reasons, its completed receipt and audit event together. Exact retries return the original outcome without resurrecting removed entries or deleting later re-additions. The same work shown through canonical aliases remains one list book.

Shared readers receive only permitted book metadata and their own library availability. Owner-only provider connections, acquisition controls and history stay private. Active list and book views refresh on focus and periodically; access errors hide previously cached content. Sharing revocation is therefore observed on refresh, not claimed to be instantaneous push delivery. Visible-only reordering preserves hidden membership slots after source/library grants change.

The interface puts books before external connection settings, keeps catalog and detail editors behind explicit actions, and uses ordinary checkbox/keyboard controls. An open metadata editor retains its captured revision and draft through polling; concurrent edits produce a reloadable conflict. No database migration, runtime dependency or new provider credential is introduced; completed `lists.curate` operations are synchronous receipts rather than worker jobs.

Verification:

- Affected curation/catalog/list-request/monitoring/policy/CSV/Hardcover/community/merge integration suite: **113 passed in 46.91 seconds** (`.local/evidence/list-curation-regression.log`). It includes atomic rollback, current owner/member authority, concurrent identical commands, changed-key rejection, canonical aliases, external exclusions, stale settings/order, membership episode fingerprints and consistent read/write snapshots.
- Final focused suite after visible-only ordering was completed: **29 passed in 8.60 seconds** (`list-curation-final-regression.log`). Hidden membership IDs are neither exposed nor required for ordering. These are affected-suite results, not a full-backend-suite claim.
- Separate ebook/audiobook acquisition cases: **2 passed in 4.40 seconds** (`list-curation-acquisition.log`). Bulk addition uses the existing automatic policy through dispatch, real-file hardlink import and delayed synthetic ABS confirmation. Removing the membership preserves the asset; replaying its original addition does not resurrect the membership or cause another transfer.

- Final browser baseline plus local curation journey: **2 passed in 1.8 minutes** (`list-curation-browser-layout.log`). It covers bulk catalog addition, order, description edits, conflicting tabs, explicit sharing, separate viewer access, revocation, keyboard bulk removal and retained library ownership. Desktop/mobile screenshots were visually inspected and retained in `.local/evidence/list-curation-ui/`.
- Python lint/format, frontend production build/format, generated OpenAPI/client and diff checks pass. The packaged wheel matches all **199 backend Python modules**. Documentation traceability remains 54 requirements and 61 packages.

Deployment: the private database/key/configuration backup and readable archive catalog were verified (`list-curation-backup-path.txt`). With zero active development jobs, the identified API and worker were stopped and restarted together. Readiness and Lists return **200**, unauthenticated list data and curation return **401**, one worker is fresh and dispatch remains **disabled** on `0038_asset_containment` (`list-curation-runtime.json`). No actual provider account or native backend certification is inferred from synthetic service fixtures.

Large-list server pagination, authoritative selectors, optional Hardcover writes, full task-based usability and actual-service qualification remain required. This advances FR-02/FR-12 within S08 and existing list lifecycle assertions; no full stage or full-PRD completion is claimed. All unmet S00–S10 obligations remain in scope.
