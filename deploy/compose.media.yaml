# All relative paths resolve from the root compose.yaml.
# Create ./data/{downloads,audiobooks,staging} and give BOOK_UID write access first.
x-media: &media
  volumes:
    - ./data:/data
services:
  api:
    <<: *media
  worker:
    <<: *media
