# Optional. The default Compose file uses inline settings and needs no .env.
# To share one password, replace both password values in compose.yaml with ${DB_PASSWORD}.
DB_PASSWORD=replace-with-your-own-password
